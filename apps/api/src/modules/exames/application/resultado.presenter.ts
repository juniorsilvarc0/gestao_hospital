/**
 * Re-export do presenter de resultado.
 *
 * O deliverable B1 lista `solicitacao.presenter.ts` e
 * `resultado.presenter.ts` separados; o conteúdo está agrupado em
 * `solicitacao.presenter.ts` (mesma família de mappers).
 */
export { presentResultado } from './solicitacao.presenter';
