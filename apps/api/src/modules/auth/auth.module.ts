/**
 * Bounded Context: Identity & Access (Auth) — placeholder Fase 1.
 * Implementação real vem na Fase 2 (login, MFA, refresh, RLS).
 */
import { Module } from '@nestjs/common';

@Module({})
export class AuthModule {}
