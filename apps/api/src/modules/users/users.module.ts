/**
 * Bounded Context: Identity & Access (Users) — placeholder Fase 1.
 * Implementação real vem na Fase 2 (CRUD de usuários, perfis).
 */
import { Module } from '@nestjs/common';

@Module({})
export class UsersModule {}
