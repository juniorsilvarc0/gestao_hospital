/**
 * Bounded Context: Patient Registry — placeholder Fase 1.
 * Implementação real na Fase 3 (cadastros gerais).
 */
import { Module } from '@nestjs/common';

@Module({})
export class PacientesModule {}
