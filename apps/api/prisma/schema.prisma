generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
  binaryTargets   = ["native", "linux-musl-arm64-openssl-3.0.x", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider          = "postgresql"
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  extensions        = [btree_gin, btree_gist, pg_trgm, pgcrypto, unaccent, uuid_ossp(map: "uuid-ossp")]
}

model Tenant {
  id                      BigInt                    @id @default(autoincrement())
  uuidExterno             String                    @default(dbgenerated("uuid_generate_v4()")) @map("uuid_externo") @db.Uuid
  cnpj                    String                    @unique @db.VarChar(18)
  razaoSocial             String                    @map("razao_social") @db.VarChar(300)
  nomeFantasia            String?                   @map("nome_fantasia") @db.VarChar(300)
  cnes                    String?                   @db.VarChar(20)
  registroAns             String?                   @map("registro_ans") @db.VarChar(20)
  configuracoes           Json                      @default("{}")
  versaoTissPadrao        String                    @default("4.01.00") @map("versao_tiss_padrao") @db.VarChar(10)
  ativo                   Boolean                   @default(true)
  createdAt               DateTime                  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt               DateTime?                 @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt               DateTime?                 @map("deleted_at") @db.Timestamptz(6)
  codigo                  String                    @unique(map: "uq_tenants_codigo") @db.VarChar(20)
  agendamentos            agendamentos[]
  agendas_bloqueios       agendas_bloqueios[]
  agendas_disponibilidade agendas_disponibilidade[]
  agendas_recursos        agendas_recursos[]
  atendimentos            atendimentos[]
  centros_custo           centros_custo[]
  condicoes_contratuais   condicoes_contratuais[]
  contas                  contas[]
  convenios               convenios[]
  equipamentos            equipamentos[]
  especialidades          especialidades[]
  import_jobs             import_jobs[]
  leitos                  leitos[]
  mfa_recovery_codes      MfaRecoveryCode[]
  pacientes               pacientes[]
  pacientes_convenios     pacientes_convenios[]
  perfis                  Perfil[]
  planos                  planos[]
  prestadores             prestadores[]
  salas_cirurgicas        salas_cirurgicas[]
  setores                 setores[]
  solicitacoes_lgpd       solicitacoes_lgpd[]
  tabelas_precos          tabelas_precos[]
  tabelas_procedimentos   tabelas_procedimentos[]
  triagens                triagens[]
  unidades_atendimento    unidades_atendimento[]
  unidades_faturamento    unidades_faturamento[]
  usuarios                Usuario[]

  @@index([codigo], map: "ix_tenants_codigo")
  @@map("tenants")
}

model Usuario {
  id                                                          BigInt                   @id @default(autoincrement())
  uuidExterno                                                 String                   @default(dbgenerated("uuid_generate_v4()")) @map("uuid_externo") @db.Uuid
  tenantId                                                    BigInt                   @map("tenant_id")
  email                                                       String                   @db.VarChar(200)
  senhaHash                                                   String?                  @map("senha_hash") @db.VarChar(255)
  mfaSecret                                                   String?                  @map("mfa_secret") @db.VarChar(255)
  mfaHabilitado                                               Boolean                  @default(false) @map("mfa_habilitado")
  prestadorId                                                 BigInt?                  @map("prestador_id")
  nome                                                        String                   @db.VarChar(300)
  ativo                                                       Boolean                  @default(true)
  ultimoLoginEm                                               DateTime?                @map("ultimo_login_em") @db.Timestamptz(6)
  ultimoLoginIp                                               String?                  @map("ultimo_login_ip") @db.Inet
  tentativasLogin                                             Int                      @default(0) @map("tentativas_login")
  bloqueadoAte                                                DateTime?                @map("bloqueado_ate") @db.Timestamptz(6)
  precisaTrocarSenha                                          Boolean                  @default(true) @map("precisa_trocar_senha")
  createdAt                                                   DateTime                 @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                                                   DateTime?                @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt                                                   DateTime?                @map("deleted_at") @db.Timestamptz(6)
  paciente_id                                                 BigInt?
  tipo_perfil                                                 enum_usuario_tipo_perfil @default(INTERNO)
  acessos_prontuario                                          acessos_prontuario[]
  agendamentos_agendamentos_cancelado_porTousuarios           agendamentos[]           @relation("agendamentos_cancelado_porTousuarios")
  agendamentos_agendamentos_checkin_porTousuarios             agendamentos[]           @relation("agendamentos_checkin_porTousuarios")
  agendamentos_agendamentos_created_byTousuarios              agendamentos[]           @relation("agendamentos_created_byTousuarios")
  agendamentos_agendamentos_updated_byTousuarios              agendamentos[]           @relation("agendamentos_updated_byTousuarios")
  agendas_bloqueios                                           agendas_bloqueios[]
  atendimentos_atendimentos_classificacao_risco_porTousuarios atendimentos[]           @relation("atendimentos_classificacao_risco_porTousuarios")
  atendimentos_atendimentos_created_byTousuarios              atendimentos[]           @relation("atendimentos_created_byTousuarios")
  atendimentos_atendimentos_deleted_byTousuarios              atendimentos[]           @relation("atendimentos_deleted_byTousuarios")
  atendimentos_atendimentos_updated_byTousuarios              atendimentos[]           @relation("atendimentos_updated_byTousuarios")
  cme_lotes                                                   cme_lotes[]
  glosas                                                      glosas[]
  import_jobs                                                 import_jobs[]
  mfaRecoveryCodes                                            MfaRecoveryCode[]
  repasses_repasses_conferido_porTousuarios                   repasses[]               @relation("repasses_conferido_porTousuarios")
  repasses_repasses_liberado_porTousuarios                    repasses[]               @relation("repasses_liberado_porTousuarios")
  repasses_repasses_pago_porTousuarios                        repasses[]               @relation("repasses_pago_porTousuarios")
  same_emprestimos                                            same_emprestimos[]
  same_prontuarios                                            same_prontuarios[]
  sessoesAtivas                                               SessaoAtiva[]
  triagens                                                    triagens[]
  usuario_setores                                             usuario_setores[]
  prestadores                                                 prestadores?             @relation(fields: [prestadorId], references: [id], onUpdate: NoAction, map: "fk_usuarios_prestador_id")
  tenant                                                      Tenant                   @relation(fields: [tenantId], references: [id], map: "fk_usuarios_tenant_id")
  pacientes                                                   pacientes?               @relation(fields: [paciente_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  perfis                                                      UsuarioPerfil[]
  visitantes                                                  visitantes[]
  visitas                                                     visitas[]

  @@unique([tenantId, email], map: "uq_usuarios_email_tenant")
  @@index([tenantId], map: "ix_usuarios_tenant")
  @@index([prestadorId], map: "ix_usuarios_prestador")
  @@map("usuarios")
}

model Perfil {
  id         BigInt            @id @default(autoincrement())
  tenantId   BigInt            @map("tenant_id")
  codigo     String            @db.VarChar(50)
  nome       String            @db.VarChar(120)
  descricao  String?           @db.VarChar(500)
  ativo      Boolean           @default(true)
  tenant     Tenant            @relation(fields: [tenantId], references: [id], map: "fk_perfis_tenant_id")
  permissoes PerfilPermissao[]
  usuarios   UsuarioPerfil[]

  @@unique([tenantId, codigo], map: "uq_perfis_codigo_tenant")
  @@map("perfis")
}

model Permissao {
  id        BigInt            @id @default(autoincrement())
  recurso   String            @db.VarChar(80)
  acao      String            @db.VarChar(40)
  descricao String?           @db.VarChar(300)
  perfis    PerfilPermissao[]

  @@unique([recurso, acao], map: "uq_permissoes")
  @@map("permissoes")
}

model UsuarioPerfil {
  usuarioId BigInt  @map("usuario_id")
  perfilId  BigInt  @map("perfil_id")
  perfil    Perfil  @relation(fields: [perfilId], references: [id], onDelete: Cascade, map: "fk_usuarios_perfis_perfil_id")
  usuario   Usuario @relation(fields: [usuarioId], references: [id], onDelete: Cascade, map: "fk_usuarios_perfis_usuario_id")

  @@id([usuarioId, perfilId])
  @@map("usuarios_perfis")
}

model PerfilPermissao {
  perfilId    BigInt    @map("perfil_id")
  permissaoId BigInt    @map("permissao_id")
  perfil      Perfil    @relation(fields: [perfilId], references: [id], onDelete: Cascade, map: "fk_perfis_permissoes_perfil_id")
  permissao   Permissao @relation(fields: [permissaoId], references: [id], onDelete: Cascade, map: "fk_perfis_permissoes_permissao_id")

  @@id([perfilId, permissaoId])
  @@map("perfis_permissoes")
}

model SessaoAtiva {
  id               String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  usuarioId        BigInt    @map("usuario_id")
  refreshTokenHash String    @map("refresh_token_hash") @db.VarChar(255)
  ip               String?   @db.Inet
  userAgent        String?   @map("user_agent")
  expiraEm         DateTime  @map("expira_em") @db.Timestamptz(6)
  revogadaEm       DateTime? @map("revogada_em") @db.Timestamptz(6)
  createdAt        DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  usuario          Usuario   @relation(fields: [usuarioId], references: [id], onDelete: Cascade, map: "fk_sessoes_ativas_usuario_id")

  @@index([usuarioId], map: "ix_sessoes_usuario")
  @@map("sessoes_ativas")
}

model MfaRecoveryCode {
  id        BigInt    @id @default(autoincrement())
  tenantId  BigInt    @map("tenant_id")
  usuarioId BigInt    @map("usuario_id")
  codeHash  String    @map("code_hash") @db.VarChar(255)
  usedAt    DateTime? @map("used_at") @db.Timestamptz(6)
  createdAt DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  tenants   Tenant    @relation(fields: [tenantId], references: [id], onUpdate: NoAction, map: "fk_mfa_recovery_codes_tenant_id")
  usuario   Usuario   @relation(fields: [usuarioId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "fk_mfa_recovery_codes_usuario_id")

  @@index([tenantId], map: "ix_mfa_rc_tenant")
  @@map("mfa_recovery_codes")
}

/// This table is a partition table and requires additional setup for migrations. Visit https://pris.ly/d/partition-tables for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model acessos_prontuario {
  id             BigInt        @default(autoincrement())
  tenant_id      BigInt
  paciente_id    BigInt
  atendimento_id BigInt?
  usuario_id     BigInt
  perfil         String        @db.VarChar(50)
  finalidade     String        @db.VarChar(200)
  modulo         String        @db.VarChar(50)
  ip             String?       @db.Inet
  acessado_em    DateTime      @default(now()) @db.Timestamptz(6)
  pacientes      pacientes     @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  usuarios       Usuario       @relation(fields: [usuario_id], references: [id], onUpdate: NoAction)
  atendimentos   atendimentos? @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction, map: "fk_acessos_atendimento_id")

  @@id([id, acessado_em])
  @@index([paciente_id, acessado_em(sort: Desc)], map: "ix_acesso_paciente")
  @@index([usuario_id, acessado_em(sort: Desc)], map: "ix_acesso_usuario")
}

/// This table is a partition table and requires additional setup for migrations. Visit https://pris.ly/d/partition-tables for more info.
/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model auditoria_eventos {
  id             BigInt   @default(autoincrement())
  tenant_id      BigInt?
  tabela         String   @db.VarChar(120)
  registro_id    BigInt
  operacao       String   @db.Char(1)
  diff           Json
  usuario_id     BigInt?
  ip             String?  @db.Inet
  user_agent     String?
  finalidade     String?  @db.VarChar(200)
  correlation_id String?  @db.Uuid
  created_at     DateTime @default(now()) @db.Timestamptz(6)

  @@id([id, created_at])
  @@index([correlation_id], map: "ix_audit_correlation")
  @@index([tenant_id, tabela, registro_id], map: "ix_audit_tenant_tabela_registro")
  @@index([usuario_id, created_at(sort: Desc)], map: "ix_audit_usuario")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model centros_custo {
  id                  BigInt          @id @default(autoincrement())
  tenant_id           BigInt
  codigo              String          @db.VarChar(20)
  nome                String          @db.VarChar(120)
  parent_id           BigInt?
  ativo               Boolean         @default(true)
  created_at          DateTime        @default(now()) @db.Timestamptz(6)
  updated_at          DateTime?       @db.Timestamptz(6)
  deleted_at          DateTime?       @db.Timestamptz(6)
  uuid_externo        String          @unique(map: "uq_centros_custo_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  centros_custo       centros_custo?  @relation("centros_custoTocentros_custo", fields: [parent_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  other_centros_custo centros_custo[] @relation("centros_custoTocentros_custo")
  tenants             Tenant          @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  setores             setores[]

  @@unique([tenant_id, codigo], map: "uq_cc")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model condicoes_contratuais {
  id                           BigInt    @id @default(autoincrement())
  tenant_id                    BigInt
  convenio_id                  BigInt
  plano_id                     BigInt?
  versao                       Int       @default(1)
  vigencia_inicio              DateTime  @db.Date
  vigencia_fim                 DateTime? @db.Date
  coberturas                   Json      @default("[]")
  especialidades_habilitadas   Json?
  agrupamentos                 Json?
  parametros_tiss              Json?
  iss_aliquota                 Decimal?  @db.Decimal(7, 4)
  iss_retem                    Boolean   @default(false)
  exige_autorizacao_internacao Boolean   @default(true)
  exige_autorizacao_opme       Boolean   @default(true)
  prazo_envio_lote_dias        Int       @default(30)
  ativo                        Boolean   @default(true)
  created_at                   DateTime  @default(now()) @db.Timestamptz(6)
  uuid_externo                 String    @unique(map: "uq_condicoes_contratuais_uuid_externo") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  convenios                    convenios @relation(fields: [convenio_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  planos                       planos?   @relation(fields: [plano_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                      Tenant    @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([convenio_id, plano_id, versao], map: "uq_cc_versao")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model convenios {
  id                       BigInt                     @id @default(autoincrement())
  tenant_id                BigInt
  codigo                   String                     @db.VarChar(20)
  nome                     String                     @db.VarChar(300)
  cnpj                     String                     @db.VarChar(18)
  registro_ans             String?                    @db.VarChar(20)
  tipo                     enum_convenio_tipo
  padrao_tiss              Boolean                    @default(true)
  versao_tiss              String                     @default("4.01.00") @db.VarChar(10)
  url_webservice           String?                    @db.VarChar(500)
  contato                  Json?
  ativo                    Boolean                    @default(true)
  created_at               DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                  @db.Timestamptz(6)
  deleted_at               DateTime?                  @db.Timestamptz(6)
  uuid_externo             String                     @unique(map: "uq_convenios_uuid_externo") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  agendamentos             agendamentos[]
  atendimentos             atendimentos[]
  condicoes_contratuais    condicoes_contratuais[]
  contas                   contas[]
  tenants                  Tenant                     @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  convenios_tabelas_precos convenios_tabelas_precos[]
  glosas                   glosas[]
  lotes_tiss               lotes_tiss[]
  pacientes_convenios      pacientes_convenios[]
  pacotes                  pacotes[]
  planos                   planos[]

  @@unique([tenant_id, cnpj], map: "uq_convenios_cnpj")
  @@unique([tenant_id, codigo], map: "uq_convenios_codigo")
}

model convenios_tabelas_precos {
  id             BigInt         @id @default(autoincrement())
  convenio_id    BigInt
  plano_id       BigInt?
  tabela_id      BigInt
  prioridade     Int            @default(1)
  ativo          Boolean        @default(true)
  created_at     DateTime       @default(now()) @db.Timestamptz(6)
  convenios      convenios      @relation(fields: [convenio_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  planos         planos?        @relation(fields: [plano_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tabelas_precos tabelas_precos @relation(fields: [tabela_id], references: [id], onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model especialidades {
  id                         BigInt                       @id @default(autoincrement())
  tenant_id                  BigInt
  codigo_cbos                String                       @db.VarChar(10)
  nome                       String                       @db.VarChar(200)
  ativo                      Boolean                      @default(true)
  tenants                    Tenant                       @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  prestadores_especialidades prestadores_especialidades[]

  @@unique([tenant_id, codigo_cbos], map: "uq_especialidades_codigo")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model leitos {
  id                                               BigInt                     @id @default(autoincrement())
  tenant_id                                        BigInt
  setor_id                                         BigInt
  codigo                                           String                     @db.VarChar(20)
  tipo_acomodacao                                  enum_leito_tipo_acomodacao
  status                                           enum_leito_status          @default(DISPONIVEL)
  paciente_id                                      BigInt?
  atendimento_id                                   BigInt?
  ocupacao_iniciada_em                             DateTime?                  @db.Timestamptz(6)
  ocupacao_prevista_fim                            DateTime?                  @db.Timestamptz(6)
  extra                                            Boolean                    @default(false)
  observacao                                       String?                    @db.VarChar(500)
  versao                                           Int                        @default(1)
  ativo                                            Boolean                    @default(true)
  created_at                                       DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at                                       DateTime?                  @db.Timestamptz(6)
  deleted_at                                       DateTime?                  @db.Timestamptz(6)
  uuid_externo                                     String                     @unique(map: "uq_leitos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  atendimentos_atendimentos_leito_idToleitos       atendimentos[]             @relation("atendimentos_leito_idToleitos")
  ccih_casos                                       ccih_casos[]
  atendimentos_leitos_atendimento_idToatendimentos atendimentos?              @relation("leitos_atendimento_idToatendimentos", fields: [atendimento_id], references: [id], onUpdate: NoAction, map: "fk_leitos_atendimento_id")
  pacientes                                        pacientes?                 @relation(fields: [paciente_id], references: [id], onUpdate: NoAction, map: "fk_leitos_paciente_id")
  setores                                          setores                    @relation(fields: [setor_id], references: [id], onUpdate: NoAction)
  tenants                                          Tenant                     @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  visitas                                          visitas[]

  @@unique([tenant_id, setor_id, codigo], map: "uq_leitos_codigo")
  @@index([setor_id, status], map: "ix_leitos_setor_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model pacientes {
  id                      BigInt                                 @id @default(autoincrement())
  uuid_externo            String                                 @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               BigInt
  codigo                  String                                 @db.VarChar(20)
  nome                    String                                 @db.VarChar(300)
  nome_social             String?                                @db.VarChar(200)
  cpf_encrypted           Bytes?
  cpf_hash                String?                                @db.VarChar(64)
  rg                      String?                                @db.VarChar(20)
  cns                     String?                                @db.VarChar(20)
  data_nascimento         DateTime                               @db.Date
  sexo                    enum_paciente_sexo
  tipo_sanguineo          String?                                @db.VarChar(5)
  nome_mae                String                                 @db.VarChar(200)
  nome_pai                String?                                @db.VarChar(200)
  estado_civil            String?                                @db.VarChar(30)
  profissao               String?                                @db.VarChar(120)
  raca_cor                String?                                @db.VarChar(30)
  nacionalidade           String?                                @db.VarChar(60)
  naturalidade_uf         String?                                @db.VarChar(2)
  naturalidade_cidade     String?                                @db.VarChar(100)
  endereco                Json                                   @default("{}")
  contatos                Json                                   @default("{}")
  foto_url                String?                                @db.VarChar(500)
  alergias                Json?
  comorbidades            Json?
  tipo_atendimento_padrao enum_paciente_tipo_atendimento_padrao?
  obito                   Boolean                                @default(false)
  data_obito              DateTime?                              @db.Date
  causa_obito_cid         String?                                @db.VarChar(10)
  consentimento_lgpd      Boolean                                @default(false)
  consentimento_lgpd_em   DateTime?                              @db.Timestamptz(6)
  paciente_mae_id         BigInt?
  campos_complementares   Json?
  created_at              DateTime                               @default(now()) @db.Timestamptz(6)
  created_by              BigInt?
  updated_at              DateTime?                              @db.Timestamptz(6)
  updated_by              BigInt?
  deleted_at              DateTime?                              @db.Timestamptz(6)
  deleted_by              BigInt?
  versao                  Int                                    @default(1)
  acessos_prontuario      acessos_prontuario[]
  agendamentos            agendamentos[]
  atendimentos            atendimentos[]
  ccih_casos              ccih_casos[]
  cirurgias               cirurgias[]
  cme_artigos             cme_artigos[]
  consentimentos_lgpd     consentimentos_lgpd[]
  contas                  contas[]
  dispensacoes            dispensacoes[]
  documentos_emitidos     documentos_emitidos[]
  leitos                  leitos[]
  livro_controlados       livro_controlados[]
  notificacoes_paciente   notificacoes_paciente[]
  pacientes               pacientes?                             @relation("pacientesTopacientes", fields: [paciente_mae_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  other_pacientes         pacientes[]                            @relation("pacientesTopacientes")
  tenants                 Tenant                                 @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  pacientes_convenios     pacientes_convenios[]
  resultados_exame        resultados_exame[]
  same_prontuarios        same_prontuarios[]
  solicitacoes_exame      solicitacoes_exame[]
  solicitacoes_lgpd       solicitacoes_lgpd[]
  usuarios                Usuario[]
  visitas                 visitas[]

  @@unique([tenant_id, cns], map: "uq_pacientes_cns_tenant")
  @@unique([tenant_id, codigo], map: "uq_pacientes_codigo_tenant")
  @@unique([tenant_id, cpf_hash], map: "uq_pacientes_cpf_tenant")
  @@index([alergias(ops: JsonbPathOps)], map: "ix_pacientes_alergias", type: Gin)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model pacientes_convenios {
  id                 BigInt         @id @default(autoincrement())
  tenant_id          BigInt
  paciente_id        BigInt
  convenio_id        BigInt
  plano_id           BigInt?
  numero_carteirinha String         @db.VarChar(40)
  validade           DateTime?      @db.Date
  titular            Boolean        @default(true)
  parentesco_titular String?        @db.VarChar(40)
  prioridade         Int            @default(1)
  ativo              Boolean        @default(true)
  created_at         DateTime       @default(now()) @db.Timestamptz(6)
  updated_at         DateTime?      @db.Timestamptz(6)
  deleted_at         DateTime?      @db.Timestamptz(6)
  uuid_externo       String         @unique(map: "uq_pac_conv_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  atendimentos       atendimentos[]
  convenios          convenios      @relation(fields: [convenio_id], references: [id], onUpdate: NoAction)
  pacientes          pacientes      @relation(fields: [paciente_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  planos             planos?        @relation(fields: [plano_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tenants            Tenant         @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, convenio_id, numero_carteirinha], map: "uq_pac_conv_carteirinha")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model planos {
  id                       BigInt                      @id @default(autoincrement())
  tenant_id                BigInt
  convenio_id              BigInt
  codigo                   String                      @db.VarChar(20)
  nome                     String                      @db.VarChar(200)
  registro_ans             String?                     @db.VarChar(20)
  tipo_acomodacao          enum_leito_tipo_acomodacao?
  segmentacao              String?                     @db.VarChar(50)
  ativo                    Boolean                     @default(true)
  created_at               DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                   @db.Timestamptz(6)
  deleted_at               DateTime?                   @db.Timestamptz(6)
  uuid_externo             String                      @unique(map: "uq_planos_uuid_externo") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  agendamentos             agendamentos[]
  atendimentos             atendimentos[]
  condicoes_contratuais    condicoes_contratuais[]
  contas                   contas[]
  convenios_tabelas_precos convenios_tabelas_precos[]
  pacientes_convenios      pacientes_convenios[]
  convenios                convenios                   @relation(fields: [convenio_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants                  Tenant                      @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([convenio_id, codigo], map: "uq_planos_codigo")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model prestadores {
  id                         BigInt                       @id @default(autoincrement())
  uuid_externo               String                       @unique(map: "uq_prestadores_uuid_externo") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                  BigInt
  nome                       String                       @db.VarChar(300)
  nome_social                String?                      @db.VarChar(200)
  cpf_hash                   String?                      @db.VarChar(64)
  tipo_conselho              enum_prestador_tipo_conselho
  numero_conselho            String                       @db.VarChar(20)
  uf_conselho                String                       @db.VarChar(2)
  rqe                        String?                      @db.VarChar(20)
  tipo_vinculo               enum_prestador_tipo_vinculo
  recebe_repasse             Boolean                      @default(true)
  repasse_diaria             Boolean                      @default(false)
  repasse_taxa               Boolean                      @default(false)
  repasse_servico            Boolean                      @default(false)
  repasse_matmed             Boolean                      @default(false)
  socio_cooperado            Boolean                      @default(false)
  credenciado_direto         Json?
  dados_bancarios            Json?
  cbo_principal              String?                      @db.VarChar(10)
  ativo                      Boolean                      @default(true)
  created_at                 DateTime                     @default(now()) @db.Timestamptz(6)
  updated_at                 DateTime?                    @db.Timestamptz(6)
  deleted_at                 DateTime?                    @db.Timestamptz(6)
  agendas_recursos           agendas_recursos[]
  analises_farmaceuticas     analises_farmaceuticas[]
  atendimentos               atendimentos[]
  cadernos_gabaritos         cadernos_gabaritos[]
  cirurgias                  cirurgias[]
  cirurgias_equipe           cirurgias_equipe[]
  cme_lotes                  cme_lotes[]
  cme_movimentacoes          cme_movimentacoes[]
  contas_itens               contas_itens[]
  dispensacoes               dispensacoes[]
  documentos_emitidos        documentos_emitidos[]
  livro_controlados          livro_controlados[]
  tenants                    Tenant                       @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  prestadores_especialidades prestadores_especialidades[]
  repasses                   repasses[]
  resultados_exame           resultados_exame[]
  solicitacoes_exame         solicitacoes_exame[]
  usuarios                   Usuario[]

  @@unique([tenant_id, tipo_conselho, numero_conselho, uf_conselho], map: "uq_prestadores_conselho")
}

model prestadores_especialidades {
  prestador_id     BigInt
  especialidade_id BigInt
  principal        Boolean        @default(false)
  rqe              String?        @db.VarChar(20)
  especialidades   especialidades @relation(fields: [especialidade_id], references: [id], onUpdate: NoAction)
  prestadores      prestadores    @relation(fields: [prestador_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([prestador_id, especialidade_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model salas_cirurgicas {
  id               BigInt             @id @default(autoincrement())
  tenant_id        BigInt
  setor_id         BigInt
  codigo           String             @db.VarChar(20)
  nome             String             @db.VarChar(120)
  tipo             String?            @db.VarChar(50)
  status           String             @default("DISPONIVEL") @db.VarChar(30)
  ativa            Boolean            @default(true)
  created_at       DateTime           @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?          @db.Timestamptz(6)
  deleted_at       DateTime?          @db.Timestamptz(6)
  uuid_externo     String             @unique(map: "uq_salas_cirurgicas_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  agendas_recursos agendas_recursos[]
  cirurgias        cirurgias[]
  setores          setores            @relation(fields: [setor_id], references: [id], onUpdate: NoAction)
  tenants          Tenant             @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, codigo], map: "uq_salas")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model setores {
  id                     BigInt               @id @default(autoincrement())
  tenant_id              BigInt
  nome                   String               @db.VarChar(120)
  tipo                   enum_setor_tipo
  unidade_faturamento_id BigInt
  unidade_atendimento_id BigInt
  centro_custo_id        BigInt?
  capacidade             Int?
  ativo                  Boolean              @default(true)
  created_at             DateTime             @default(now()) @db.Timestamptz(6)
  updated_at             DateTime?            @db.Timestamptz(6)
  deleted_at             DateTime?            @db.Timestamptz(6)
  uuid_externo           String               @unique(map: "uq_setores_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  atendimentos           atendimentos[]
  ccih_casos             ccih_casos[]
  contas_itens           contas_itens[]
  dispensacoes           dispensacoes[]
  equipamentos           equipamentos[]
  leitos                 leitos[]
  salas_cirurgicas       salas_cirurgicas[]
  centros_custo          centros_custo?       @relation(fields: [centro_custo_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tenants                Tenant               @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  unidades_atendimento   unidades_atendimento @relation(fields: [unidade_atendimento_id], references: [id], onUpdate: NoAction)
  unidades_faturamento   unidades_faturamento @relation(fields: [unidade_faturamento_id], references: [id], onUpdate: NoAction)
  usuario_setores        usuario_setores[]
  visitas                visitas[]

  @@unique([tenant_id, nome], map: "uq_setores_nome")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tabelas_precos {
  id                       BigInt                     @id @default(autoincrement())
  tenant_id                BigInt
  codigo                   String                     @db.VarChar(40)
  nome                     String                     @db.VarChar(200)
  vigencia_inicio          DateTime                   @db.Date
  vigencia_fim             DateTime?                  @db.Date
  versao                   Int                        @default(1)
  ativa                    Boolean                    @default(true)
  created_at               DateTime                   @default(now()) @db.Timestamptz(6)
  convenios_tabelas_precos convenios_tabelas_precos[]
  tenants                  Tenant                     @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  tabelas_precos_itens     tabelas_precos_itens[]

  @@unique([tenant_id, codigo, versao], map: "uq_tp_codigo")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model tabelas_precos_itens {
  id                    BigInt                @id @default(autoincrement())
  tenant_id             BigInt
  tabela_id             BigInt
  procedimento_id       BigInt
  valor                 Decimal               @db.Decimal(18, 4)
  valor_filme           Decimal?              @db.Decimal(18, 4)
  porte_anestesico      String?               @db.VarChar(10)
  tempo_minutos         Int?
  custo_operacional     Decimal?              @db.Decimal(18, 4)
  observacao            String?               @db.VarChar(500)
  tabelas_procedimentos tabelas_procedimentos @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)
  tabelas_precos        tabelas_precos        @relation(fields: [tabela_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tabela_id, procedimento_id], map: "uq_tpi")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model tabelas_procedimentos {
  id                           BigInt                         @id @default(autoincrement())
  tenant_id                    BigInt
  codigo_tuss                  String                         @db.VarChar(20)
  codigo_cbhpm                 String?                        @db.VarChar(20)
  codigo_amb                   String?                        @db.VarChar(20)
  codigo_sus                   String?                        @db.VarChar(20)
  codigo_anvisa                String?                        @db.VarChar(20)
  codigo_ean                   String?                        @db.VarChar(20)
  nome                         String                         @db.VarChar(500)
  nome_reduzido                String?                        @db.VarChar(120)
  tipo                         enum_procedimento_tipo
  grupo_gasto                  enum_grupo_gasto
  tabela_tiss                  String?                        @db.VarChar(10)
  unidade_medida               String?                        @db.VarChar(20)
  fator_conversao              Decimal?                       @default(1.0) @db.Decimal(18, 6)
  valor_referencia             Decimal?                       @db.Decimal(18, 4)
  porte                        String?                        @db.VarChar(10)
  custo_operacional            Decimal?                       @db.Decimal(18, 4)
  precisa_autorizacao          Boolean                        @default(false)
  precisa_assinatura           Boolean                        @default(false)
  precisa_lote                 Boolean                        @default(false)
  controlado                   Boolean                        @default(false)
  alto_custo                   Boolean                        @default(false)
  ativo                        Boolean                        @default(true)
  created_at                   DateTime                       @default(now()) @db.Timestamptz(6)
  updated_at                   DateTime?                      @db.Timestamptz(6)
  uuid_externo                 String                         @unique(map: "uq_tabelas_procedimentos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  agendamentos                 agendamentos[]
  cadernos_gabaritos           cadernos_gabaritos[]
  cadernos_gabaritos_itens     cadernos_gabaritos_itens[]
  cirurgias                    cirurgias[]
  contas_itens                 contas_itens[]
  dispensacoes_itens           dispensacoes_itens[]
  kits_cirurgicos_itens        kits_cirurgicos_itens[]
  livro_controlados            livro_controlados[]
  pacotes                      pacotes[]
  pacotes_itens                pacotes_itens[]
  prescricoes_itens            prescricoes_itens[]
  procedimento_principio_ativo procedimento_principio_ativo[]
  solicitacoes_exame_itens     solicitacoes_exame_itens[]
  tabelas_precos_itens         tabelas_precos_itens[]
  tenants                      Tenant                         @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, codigo_tuss], map: "uq_proc_tuss")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model unidades_atendimento {
  id                BigInt              @id @default(autoincrement())
  tenant_id         BigInt
  codigo            String              @db.VarChar(20)
  nome              String              @db.VarChar(120)
  ativa             Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  deleted_at        DateTime?           @db.Timestamptz(6)
  uuid_externo      String              @unique(map: "uq_unidades_atendimento_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  atendimentos      atendimentos[]
  criterios_repasse criterios_repasse[]
  setores           setores[]
  tenants           Tenant              @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, codigo], map: "uq_ua")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model unidades_faturamento {
  id                BigInt              @id @default(autoincrement())
  tenant_id         BigInt
  codigo            String              @db.VarChar(20)
  nome              String              @db.VarChar(120)
  cnes              String?             @db.VarChar(20)
  ativa             Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime?           @db.Timestamptz(6)
  deleted_at        DateTime?           @db.Timestamptz(6)
  uuid_externo      String              @unique(map: "uq_unidades_faturamento_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  atendimentos      atendimentos[]
  criterios_repasse criterios_repasse[]
  setores           setores[]
  tenants           Tenant              @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, codigo], map: "uq_uf")
}

model usuario_setores {
  usuario_id BigInt
  setor_id   BigInt
  setores    setores @relation(fields: [setor_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  usuarios   Usuario @relation(fields: [usuario_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([usuario_id, setor_id])
}

/// LGPD — pedidos de acesso/exclusão/portabilidade/correção (RN-LGP-02..03).
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model solicitacoes_lgpd {
  id             BigInt                       @id @default(autoincrement())
  uuid_externo   String                       @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id      BigInt
  paciente_id    BigInt
  tipo           enum_lgpd_solicitacao_tipo
  motivo         String?
  status         enum_lgpd_solicitacao_status @default(PENDENTE)
  prazo_sla_dias Int                          @default(15)
  solicitada_em  DateTime                     @default(now()) @db.Timestamptz(6)
  solicitante_id BigInt?
  atendida_em    DateTime?                    @db.Timestamptz(6)
  atendida_por   BigInt?
  resposta       String?
  created_at     DateTime                     @default(now()) @db.Timestamptz(6)
  updated_at     DateTime?                    @db.Timestamptz(6)
  deleted_at     DateTime?                    @db.Timestamptz(6)
  pacientes      pacientes                    @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  tenants        Tenant                       @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
}

/// Jobs assíncronos de importação de catálogos (TUSS/CBHPM/CID-10/CBO).
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model import_jobs {
  id           BigInt    @id @default(autoincrement())
  uuid_externo String    @unique(map: "uq_import_jobs_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id    BigInt
  tipo         String    @db.VarChar(40)
  arquivo_nome String?   @db.VarChar(300)
  arquivo_url  String?   @db.VarChar(500)
  status       String    @default("PENDENTE") @db.VarChar(40)
  total        Int       @default(0)
  processados  Int       @default(0)
  erros        Int       @default(0)
  error_log    Json?
  iniciado_em  DateTime? @db.Timestamptz(6)
  concluido_em DateTime? @db.Timestamptz(6)
  iniciado_por BigInt?
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  usuarios     Usuario?  @relation(fields: [iniciado_por], references: [id], onUpdate: NoAction, map: "fk_import_jobs_iniciado_por")
  tenants      Tenant    @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "fk_import_jobs_tenant_id")

  @@index([tenant_id, status], map: "ix_import_jobs_status")
  @@index([tenant_id, created_at(sort: Desc)], map: "ix_import_jobs_created_desc")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This table contains exclusion constraints and requires additional setup for migrations. Visit https://pris.ly/d/exclusion-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model agendamentos {
  id                                            BigInt                  @id @default(autoincrement())
  uuid_externo                                  String                  @unique(map: "uq_agendamentos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                     BigInt
  paciente_id                                   BigInt
  recurso_id                                    BigInt
  procedimento_id                               BigInt?
  inicio                                        DateTime                @db.Timestamptz(6)
  fim                                           DateTime                @db.Timestamptz(6)
  tipo                                          enum_atendimento_tipo
  status                                        enum_agendamento_status @default(AGENDADO)
  origem                                        enum_agendamento_origem @default(INTERNO)
  encaixe                                       Boolean                 @default(false)
  encaixe_motivo                                String?                 @db.VarChar(300)
  convenio_id                                   BigInt?
  plano_id                                      BigInt?
  observacao                                    String?                 @db.VarChar(500)
  link_teleconsulta                             String?                 @db.VarChar(500)
  teleconsulta_nonce                            String?                 @db.VarChar(80)
  confirmado_em                                 DateTime?               @db.Timestamptz(6)
  confirmado_por                                BigInt?
  confirmado_via                                String?                 @db.VarChar(40)
  checkin_em                                    DateTime?               @db.Timestamptz(6)
  checkin_por                                   BigInt?
  no_show_marcado_em                            DateTime?               @db.Timestamptz(6)
  cancelado_em                                  DateTime?               @db.Timestamptz(6)
  cancelado_por                                 BigInt?
  cancelamento_motivo                           String?                 @db.VarChar(300)
  reagendado_para_id                            BigInt?
  created_at                                    DateTime                @default(now()) @db.Timestamptz(6)
  created_by                                    BigInt?
  updated_at                                    DateTime?               @db.Timestamptz(6)
  updated_by                                    BigInt?
  versao                                        Int                     @default(1)
  usuarios_agendamentos_cancelado_porTousuarios Usuario?                @relation("agendamentos_cancelado_porTousuarios", fields: [cancelado_por], references: [id], onUpdate: NoAction)
  usuarios_agendamentos_checkin_porTousuarios   Usuario?                @relation("agendamentos_checkin_porTousuarios", fields: [checkin_por], references: [id], onUpdate: NoAction)
  convenios                                     convenios?              @relation(fields: [convenio_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  usuarios_agendamentos_created_byTousuarios    Usuario?                @relation("agendamentos_created_byTousuarios", fields: [created_by], references: [id], onUpdate: NoAction)
  pacientes                                     pacientes               @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  planos                                        planos?                 @relation(fields: [plano_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tabelas_procedimentos                         tabelas_procedimentos?  @relation(fields: [procedimento_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  agendamentos                                  agendamentos?           @relation("agendamentosToagendamentos", fields: [reagendado_para_id], references: [id], onUpdate: NoAction)
  other_agendamentos                            agendamentos[]          @relation("agendamentosToagendamentos")
  agendas_recursos                              agendas_recursos        @relation(fields: [recurso_id], references: [id], onUpdate: NoAction)
  tenants                                       Tenant                  @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  usuarios_agendamentos_updated_byTousuarios    Usuario?                @relation("agendamentos_updated_byTousuarios", fields: [updated_by], references: [id], onUpdate: NoAction)
  atendimentos                                  atendimentos[]

  @@index([paciente_id, inicio(sort: Desc)], map: "ix_agend_paciente")
  @@index([recurso_id, inicio], map: "ix_agend_recurso_inicio")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This table contains exclusion constraints and requires additional setup for migrations. Visit https://pris.ly/d/exclusion-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model agendas_bloqueios {
  id               BigInt           @id @default(autoincrement())
  tenant_id        BigInt
  recurso_id       BigInt
  inicio           DateTime         @db.Timestamptz(6)
  fim              DateTime         @db.Timestamptz(6)
  motivo           String?          @db.VarChar(200)
  criado_por       BigInt?
  created_at       DateTime         @default(now()) @db.Timestamptz(6)
  usuarios         Usuario?         @relation(fields: [criado_por], references: [id], onUpdate: NoAction)
  agendas_recursos agendas_recursos @relation(fields: [recurso_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          Tenant           @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@index([recurso_id, inicio], map: "ix_bloqueios_recurso")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model agendas_disponibilidade {
  id               BigInt           @id @default(autoincrement())
  tenant_id        BigInt
  recurso_id       BigInt
  dia_semana       Int?
  data_especifica  DateTime?        @db.Date
  hora_inicio      DateTime         @db.Time(6)
  hora_fim         DateTime         @db.Time(6)
  vigencia_inicio  DateTime?        @db.Date
  vigencia_fim     DateTime?        @db.Date
  ativa            Boolean          @default(true)
  created_at       DateTime         @default(now()) @db.Timestamptz(6)
  agendas_recursos agendas_recursos @relation(fields: [recurso_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tenants          Tenant           @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model agendas_recursos {
  id                      BigInt                    @id @default(autoincrement())
  uuid_externo            String                    @unique(map: "uq_agendas_recursos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               BigInt
  tipo                    enum_agenda_recurso_tipo
  prestador_id            BigInt?
  sala_id                 BigInt?
  equipamento_id          BigInt?
  intervalo_minutos       Int                       @default(30)
  permite_encaixe         Boolean                   @default(true)
  encaixe_max_dia         Int                       @default(2)
  ativo                   Boolean                   @default(true)
  observacao              String?                   @db.VarChar(500)
  created_at              DateTime                  @default(now()) @db.Timestamptz(6)
  updated_at              DateTime?                 @db.Timestamptz(6)
  deleted_at              DateTime?                 @db.Timestamptz(6)
  agendamentos            agendamentos[]
  agendas_bloqueios       agendas_bloqueios[]
  agendas_disponibilidade agendas_disponibilidade[]
  equipamentos            equipamentos?             @relation(fields: [equipamento_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  prestadores             prestadores?              @relation(fields: [prestador_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  salas_cirurgicas        salas_cirurgicas?         @relation(fields: [sala_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tenants                 Tenant                    @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model equipamentos {
  id               BigInt             @id @default(autoincrement())
  uuid_externo     String             @unique(map: "uq_equipamentos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id        BigInt
  setor_id         BigInt?
  codigo           String             @db.VarChar(40)
  nome             String             @db.VarChar(200)
  tipo             String?            @db.VarChar(50)
  modelo           String?            @db.VarChar(120)
  serial           String?            @db.VarChar(60)
  status           String             @default("DISPONIVEL") @db.VarChar(30)
  ativo            Boolean            @default(true)
  created_at       DateTime           @default(now()) @db.Timestamptz(6)
  updated_at       DateTime?          @db.Timestamptz(6)
  deleted_at       DateTime?          @db.Timestamptz(6)
  agendas_recursos agendas_recursos[]
  setores          setores?           @relation(fields: [setor_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tenants          Tenant             @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, codigo], map: "uq_equipamentos")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model atendimentos {
  id                                                      BigInt                                @id @default(autoincrement())
  uuid_externo                                            String                                @unique(map: "uq_atendimentos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                               BigInt
  numero_atendimento                                      String                                @db.VarChar(30)
  paciente_id                                             BigInt
  tipo                                                    enum_atendimento_tipo
  data_hora_entrada                                       DateTime                              @default(now()) @db.Timestamptz(6)
  data_hora_saida                                         DateTime?                             @db.Timestamptz(6)
  prestador_id                                            BigInt
  setor_id                                                BigInt
  unidade_faturamento_id                                  BigInt
  unidade_atendimento_id                                  BigInt
  leito_id                                                BigInt?
  tipo_cobranca                                           enum_tipo_cobranca
  paciente_convenio_id                                    BigInt?
  convenio_id                                             BigInt?
  plano_id                                                BigInt?
  numero_carteirinha                                      String?                               @db.VarChar(40)
  numero_guia_operadora                                   String?                               @db.VarChar(40)
  senha_autorizacao                                       String?                               @db.VarChar(40)
  classificacao_risco                                     enum_atendimento_classificacao_risco?
  classificacao_risco_em                                  DateTime?                             @db.Timestamptz(6)
  classificacao_risco_por                                 BigInt?
  cid_principal                                           String?                               @db.VarChar(10)
  cids_secundarios                                        Json?
  motivo_atendimento                                      String?                               @db.VarChar(500)
  tipo_alta                                               enum_atendimento_tipo_alta?
  status                                                  enum_atendimento_status               @default(EM_ESPERA)
  conta_id                                                BigInt?
  agendamento_id                                          BigInt?
  atendimento_origem_id                                   BigInt?
  observacao                                              String?
  created_at                                              DateTime                              @default(now()) @db.Timestamptz(6)
  created_by                                              BigInt?
  updated_at                                              DateTime?                             @db.Timestamptz(6)
  updated_by                                              BigInt?
  deleted_at                                              DateTime?                             @db.Timestamptz(6)
  deleted_by                                              BigInt?
  versao                                                  Int                                   @default(1)
  acessos_prontuario                                      acessos_prontuario[]
  agendamentos                                            agendamentos?                         @relation(fields: [agendamento_id], references: [id], onUpdate: NoAction)
  atendimentos                                            atendimentos?                         @relation("atendimentosToatendimentos", fields: [atendimento_origem_id], references: [id], onUpdate: NoAction)
  other_atendimentos                                      atendimentos[]                        @relation("atendimentosToatendimentos")
  usuarios_atendimentos_classificacao_risco_porTousuarios Usuario?                              @relation("atendimentos_classificacao_risco_porTousuarios", fields: [classificacao_risco_por], references: [id], onUpdate: NoAction)
  convenios                                               convenios?                            @relation(fields: [convenio_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  usuarios_atendimentos_created_byTousuarios              Usuario?                              @relation("atendimentos_created_byTousuarios", fields: [created_by], references: [id], onUpdate: NoAction)
  usuarios_atendimentos_deleted_byTousuarios              Usuario?                              @relation("atendimentos_deleted_byTousuarios", fields: [deleted_by], references: [id], onUpdate: NoAction)
  leitos_atendimentos_leito_idToleitos                    leitos?                               @relation("atendimentos_leito_idToleitos", fields: [leito_id], references: [id], onUpdate: NoAction)
  pacientes_convenios                                     pacientes_convenios?                  @relation(fields: [paciente_convenio_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  pacientes                                               pacientes                             @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  planos                                                  planos?                               @relation(fields: [plano_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  prestadores                                             prestadores                           @relation(fields: [prestador_id], references: [id], onUpdate: NoAction)
  setores                                                 setores                               @relation(fields: [setor_id], references: [id], onUpdate: NoAction)
  tenants                                                 Tenant                                @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  unidades_atendimento                                    unidades_atendimento                  @relation(fields: [unidade_atendimento_id], references: [id], onUpdate: NoAction)
  unidades_faturamento                                    unidades_faturamento                  @relation(fields: [unidade_faturamento_id], references: [id], onUpdate: NoAction)
  usuarios_atendimentos_updated_byTousuarios              Usuario?                              @relation("atendimentos_updated_byTousuarios", fields: [updated_by], references: [id], onUpdate: NoAction)
  ccih_casos                                              ccih_casos[]
  cirurgias                                               cirurgias[]
  contas                                                  contas?
  dispensacoes                                            dispensacoes[]
  documentos_emitidos                                     documentos_emitidos[]
  leitos_leitos_atendimento_idToatendimentos              leitos[]                              @relation("leitos_atendimento_idToatendimentos")
  solicitacoes_exame                                      solicitacoes_exame[]
  triagens                                                triagens[]
  visitas                                                 visitas[]

  @@unique([tenant_id, numero_atendimento], map: "uq_atendimentos_numero")
  @@index([prestador_id, data_hora_entrada(sort: Desc)], map: "ix_atend_prestador")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model contas {
  id                       BigInt             @id @default(autoincrement())
  uuid_externo             String             @unique(map: "uq_contas_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                BigInt
  numero_conta             String             @db.VarChar(30)
  atendimento_id           BigInt             @unique(map: "uq_contas_atend")
  paciente_id              BigInt
  convenio_id              BigInt?
  plano_id                 BigInt?
  tipo_cobranca            enum_tipo_cobranca
  data_abertura            DateTime           @default(now()) @db.Timestamptz(6)
  data_fechamento          DateTime?          @db.Timestamptz(6)
  data_envio               DateTime?          @db.Timestamptz(6)
  valor_total              Decimal            @default(0) @db.Decimal(18, 4)
  valor_glosa              Decimal            @default(0) @db.Decimal(18, 4)
  valor_recurso_revertido  Decimal            @default(0) @db.Decimal(18, 4)
  valor_pago               Decimal            @default(0) @db.Decimal(18, 4)
  valor_liquido            Decimal            @default(0) @db.Decimal(18, 4)
  status                   enum_conta_status  @default(ABERTA)
  observacao_elaboracao    String?
  created_at               DateTime           @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?          @db.Timestamptz(6)
  deleted_at               DateTime?          @db.Timestamptz(6)
  versao                   Int                @default(1)
  versao_tiss_snapshot     String?            @db.VarChar(10)
  condicao_contratual_snap Json?
  tabela_precos_snap       Json?
  valor_procedimentos      Decimal            @default(0) @db.Decimal(18, 4)
  valor_diarias            Decimal            @default(0) @db.Decimal(18, 4)
  valor_taxas              Decimal            @default(0) @db.Decimal(18, 4)
  valor_servicos           Decimal            @default(0) @db.Decimal(18, 4)
  valor_materiais          Decimal            @default(0) @db.Decimal(18, 4)
  valor_medicamentos       Decimal            @default(0) @db.Decimal(18, 4)
  valor_opme               Decimal            @default(0) @db.Decimal(18, 4)
  valor_gases              Decimal            @default(0) @db.Decimal(18, 4)
  valor_pacotes            Decimal            @default(0) @db.Decimal(18, 4)
  valor_honorarios         Decimal            @default(0) @db.Decimal(18, 4)
  iss_aliquota_snap        Decimal?           @db.Decimal(7, 4)
  iss_valor                Decimal?           @db.Decimal(18, 4)
  iss_retem                Boolean            @default(false)
  numero_guia_principal    String?            @db.VarChar(30)
  data_elaboracao_inicio   DateTime?          @db.Timestamptz(6)
  data_elaboracao_fim      DateTime?          @db.Timestamptz(6)
  inconsistencias          Json?
  cirurgias                cirurgias[]
  atendimentos             atendimentos       @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction)
  convenios                convenios?         @relation(fields: [convenio_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  pacientes                pacientes          @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  planos                   planos?            @relation(fields: [plano_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tenants                  Tenant             @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  contas_itens             contas_itens[]
  glosas                   glosas[]
  guias_tiss               guias_tiss[]
  repasses_itens           repasses_itens[]

  @@unique([tenant_id, numero_conta], map: "uq_contas_numero")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model triagens {
  id               BigInt                               @id @default(autoincrement())
  uuid_externo     String                               @unique(map: "uq_triagens_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id        BigInt
  atendimento_id   BigInt
  classificacao    enum_atendimento_classificacao_risco
  protocolo        String                               @default("MANCHESTER") @db.VarChar(40)
  queixa_principal String
  pa_sistolica     Int?
  pa_diastolica    Int?
  fc               Int?
  fr               Int?
  temperatura      Decimal?                             @db.Decimal(4, 1)
  sat_o2           Int?
  glicemia         Int?
  peso_kg          Decimal?                             @db.Decimal(5, 2)
  altura_cm        Int?
  dor_eva          Int?                                 @db.SmallInt
  observacao       String?
  triagem_em       DateTime                             @default(now()) @db.Timestamptz(6)
  triagem_por      BigInt
  created_at       DateTime                             @default(now()) @db.Timestamptz(6)
  atendimentos     atendimentos                         @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction)
  tenants          Tenant                               @relation(fields: [tenant_id], references: [id], onUpdate: NoAction)
  usuarios         Usuario                              @relation(fields: [triagem_por], references: [id], onUpdate: NoAction)

  @@index([atendimento_id, triagem_em(sort: Desc)], map: "ix_triagens_atendimento")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model analises_farmaceuticas {
  id                   BigInt                           @id @default(autoincrement())
  uuid_externo         String                           @unique(map: "uq_analises_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id            BigInt
  prescricao_id        BigInt
  prescricao_data_hora DateTime                         @db.Timestamptz(6)
  farmaceutico_id      BigInt
  status               enum_analise_farmaceutica_status
  parecer              String?
  ressalvas            Json?
  data_hora            DateTime                         @default(now()) @db.Timestamptz(6)
  created_by           BigInt
  prestadores          prestadores                      @relation(fields: [farmaceutico_id], references: [id], onUpdate: NoAction)

  @@index([prescricao_id, data_hora(sort: Desc)], map: "ix_analises_prescricao")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model documentos_emitidos {
  id                 BigInt              @id @default(autoincrement())
  uuid_externo       String              @unique(map: "uq_documentos_emitidos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          BigInt
  atendimento_id     BigInt?
  paciente_id        BigInt
  emissor_id         BigInt
  tipo               enum_documento_tipo
  conteudo           Json
  pdf_url            String?             @db.VarChar(500)
  assinatura_digital Json?
  assinado_em        DateTime?           @db.Timestamptz(6)
  data_emissao       DateTime            @default(now()) @db.Timestamptz(6)
  validade_dias      Int?
  versao_anterior_id BigInt?
  created_at         DateTime            @default(now()) @db.Timestamptz(6)
  atendimentos       atendimentos?       @relation(fields: [atendimento_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  prestadores        prestadores         @relation(fields: [emissor_id], references: [id], onUpdate: NoAction)
  pacientes          pacientes           @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)

  @@index([emissor_id, data_emissao(sort: Desc)], map: "ix_docs_emissor")
  @@index([paciente_id, data_emissao(sort: Desc)], map: "ix_docs_paciente")
}

/// This table is a partition table and requires additional setup for migrations. Visit https://pris.ly/d/partition-tables for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model evolucoes {
  id                 BigInt                          @default(autoincrement())
  uuid_externo       String                          @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          BigInt
  atendimento_id     BigInt
  paciente_id        BigInt
  profissional_id    BigInt
  tipo_profissional  enum_evolucao_tipo_profissional
  tipo               enum_evolucao_tipo
  data_hora          DateTime                        @db.Timestamptz(6)
  conteudo           Json
  conteudo_html      String?
  texto_livre        String?
  cids               Json?
  sinais_vitais      Json?
  assinatura_digital Json?
  assinada_em        DateTime?                       @db.Timestamptz(6)
  versao_anterior_id BigInt?
  created_at         DateTime                        @default(now()) @db.Timestamptz(6)
  created_by         BigInt
  updated_at         DateTime?                       @db.Timestamptz(6)
  updated_by         BigInt?
  deleted_at         DateTime?                       @db.Timestamptz(6)

  @@id([id, data_hora])
  @@unique([uuid_externo, data_hora], map: "uq_evolucoes_uuid")
  @@index([atendimento_id, data_hora(sort: Desc)], map: "ix_evol_atend")
  @@index([paciente_id, data_hora(sort: Desc)], map: "ix_evol_paciente")
  @@index([profissional_id, data_hora(sort: Desc)], map: "ix_evol_profissional")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model interacoes_medicamentosas {
  id                                                                         BigInt                    @id @default(autoincrement())
  principio_a                                                                BigInt
  principio_b                                                                BigInt
  severidade                                                                 enum_interacao_severidade
  descricao                                                                  String
  fonte                                                                      String?                   @db.VarChar(120)
  ativa                                                                      Boolean                   @default(true)
  created_at                                                                 DateTime                  @default(now()) @db.Timestamptz(6)
  principios_ativos_interacoes_medicamentosas_principio_aToprincipios_ativos principios_ativos         @relation("interacoes_medicamentosas_principio_aToprincipios_ativos", fields: [principio_a], references: [id], onDelete: Cascade, onUpdate: NoAction)
  principios_ativos_interacoes_medicamentosas_principio_bToprincipios_ativos principios_ativos         @relation("interacoes_medicamentosas_principio_bToprincipios_ativos", fields: [principio_b], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([principio_a, principio_b], map: "uq_interacao_par")
}

/// This table is a partition table and requires additional setup for migrations. Visit https://pris.ly/d/partition-tables for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model prescricoes {
  id                     BigInt                 @default(autoincrement())
  uuid_externo           String                 @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id              BigInt
  atendimento_id         BigInt
  paciente_id            BigInt
  prescritor_id          BigInt
  data_hora              DateTime               @db.Timestamptz(6)
  tipo                   enum_prescricao_tipo
  validade_inicio        DateTime               @db.Timestamptz(6)
  validade_fim           DateTime?              @db.Timestamptz(6)
  status                 enum_prescricao_status @default(AGUARDANDO_ANALISE)
  prescricao_anterior_id BigInt?
  observacao_geral       String?
  assinatura_digital     Json?
  assinada_em            DateTime?              @db.Timestamptz(6)
  suspensa_em            DateTime?              @db.Timestamptz(6)
  suspensa_por           BigInt?
  suspensa_motivo        String?                @db.VarChar(500)
  created_at             DateTime               @default(now()) @db.Timestamptz(6)
  created_by             BigInt?
  updated_at             DateTime?              @db.Timestamptz(6)

  @@id([id, data_hora])
  @@unique([uuid_externo, data_hora], map: "uq_prescricoes_uuid")
  @@index([atendimento_id, data_hora(sort: Desc)], map: "ix_prescr_atend")
  @@index([paciente_id, data_hora(sort: Desc)], map: "ix_prescr_paciente")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model prescricoes_itens {
  id                    BigInt                @id @default(autoincrement())
  uuid_externo          String                @unique(map: "uq_prescricoes_itens_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             BigInt
  prescricao_id         BigInt
  prescricao_data_hora  DateTime              @db.Timestamptz(6)
  procedimento_id       BigInt
  quantidade            Decimal               @db.Decimal(18, 6)
  unidade_medida        String?               @db.VarChar(20)
  dose                  String?               @db.VarChar(50)
  via                   String?               @db.VarChar(40)
  frequencia            String?               @db.VarChar(50)
  horarios              Json?
  duracao_dias          Int?
  urgente               Boolean               @default(false)
  se_necessario         Boolean               @default(false)
  observacao            String?               @db.VarChar(500)
  alerta_alergia        Json?
  alerta_interacao      Json?
  alerta_dose_max       Json?
  status_item           String                @default("ATIVO") @db.VarChar(30)
  dispensacoes_itens    dispensacoes_itens[]
  tabelas_procedimentos tabelas_procedimentos @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)

  @@index([prescricao_id], map: "ix_pi_prescricao")
  @@index([procedimento_id], map: "ix_pi_proc")
}

model principios_ativos {
  id                                                                                 BigInt                         @id @default(autoincrement())
  uuid_externo                                                                       String                         @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  nome                                                                               String                         @unique(map: "uq_principios_ativos_nome") @db.VarChar(200)
  nome_ingles                                                                        String?                        @db.VarChar(200)
  classe_atc                                                                         String?                        @db.VarChar(20)
  dose_max_dia                                                                       Decimal?                       @db.Decimal(18, 4)
  unidade_dose                                                                       String?                        @db.VarChar(20)
  observacao                                                                         String?                        @db.VarChar(500)
  ativo                                                                              Boolean                        @default(true)
  created_at                                                                         DateTime                       @default(now()) @db.Timestamptz(6)
  interacoes_medicamentosas_interacoes_medicamentosas_principio_aToprincipios_ativos interacoes_medicamentosas[]    @relation("interacoes_medicamentosas_principio_aToprincipios_ativos")
  interacoes_medicamentosas_interacoes_medicamentosas_principio_bToprincipios_ativos interacoes_medicamentosas[]    @relation("interacoes_medicamentosas_principio_bToprincipios_ativos")
  procedimento_principio_ativo                                                       procedimento_principio_ativo[]
}

model procedimento_principio_ativo {
  procedimento_id       BigInt
  principio_id          BigInt
  principios_ativos     principios_ativos     @relation(fields: [principio_id], references: [id], onUpdate: NoAction)
  tabelas_procedimentos tabelas_procedimentos @relation(fields: [procedimento_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@id([procedimento_id, principio_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model resultados_exame {
  id                                                                                      BigInt                        @id @default(autoincrement())
  uuid_externo                                                                            String                        @unique(map: "uq_resultados_exame_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                                                               BigInt
  solicitacao_item_id                                                                     BigInt
  paciente_id                                                                             BigInt
  data_coleta                                                                             DateTime?                     @db.Timestamptz(6)
  data_processamento                                                                      DateTime?                     @db.Timestamptz(6)
  data_laudo                                                                              DateTime?                     @db.Timestamptz(6)
  laudista_id                                                                             BigInt?
  laudo_estruturado                                                                       Json?
  laudo_texto                                                                             String?
  laudo_pdf_url                                                                           String?                       @db.VarChar(500)
  imagens_urls                                                                            Json?
  status                                                                                  enum_solicitacao_exame_status
  assinatura_digital                                                                      Json?
  assinado_em                                                                             DateTime?                     @db.Timestamptz(6)
  versao_anterior_id                                                                      BigInt?
  created_at                                                                              DateTime                      @default(now()) @db.Timestamptz(6)
  prestadores                                                                             prestadores?                  @relation(fields: [laudista_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  pacientes                                                                               pacientes                     @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  solicitacoes_exame_itens_resultados_exame_solicitacao_item_idTosolicitacoes_exame_itens solicitacoes_exame_itens      @relation("resultados_exame_solicitacao_item_idTosolicitacoes_exame_itens", fields: [solicitacao_item_id], references: [id], onUpdate: NoAction)
  solicitacoes_exame_itens_solicitacoes_exame_itens_resultado_idToresultados_exame        solicitacoes_exame_itens[]    @relation("solicitacoes_exame_itens_resultado_idToresultados_exame")

  @@index([paciente_id, data_laudo(sort: Desc)], map: "ix_res_exame_paciente")
}

/// This table is a partition table and requires additional setup for migrations. Visit https://pris.ly/d/partition-tables for more info.
/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model sinais_vitais {
  id               BigInt   @default(autoincrement())
  uuid_externo     String   @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id        BigInt
  atendimento_id   BigInt
  paciente_id      BigInt
  registrado_por   BigInt
  data_hora        DateTime @db.Timestamptz(6)
  pa_sistolica     Int?
  pa_diastolica    Int?
  fc               Int?
  fr               Int?
  temperatura      Decimal? @db.Decimal(4, 1)
  sat_o2           Int?
  glicemia         Int?
  peso_kg          Decimal? @db.Decimal(5, 2)
  altura_cm        Int?
  dor_eva          Int?     @db.SmallInt
  observacao       String?
  valor_confirmado Boolean  @default(false)
  justificativa    String?  @db.VarChar(500)
  created_at       DateTime @default(now()) @db.Timestamptz(6)

  @@id([id, data_hora])
  @@index([atendimento_id, data_hora(sort: Desc)], map: "ix_sv_atend")
  @@index([paciente_id, data_hora(sort: Desc)], map: "ix_sv_paciente")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model solicitacoes_exame {
  id                       BigInt                          @id @default(autoincrement())
  uuid_externo             String                          @unique(map: "uq_solicitacoes_exame_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                BigInt
  atendimento_id           BigInt
  paciente_id              BigInt
  solicitante_id           BigInt
  urgencia                 enum_solicitacao_exame_urgencia @default(ROTINA)
  indicacao_clinica        String
  numero_guia              String?                         @db.VarChar(30)
  status                   enum_solicitacao_exame_status   @default(SOLICITADO)
  data_solicitacao         DateTime                        @default(now()) @db.Timestamptz(6)
  data_realizacao          DateTime?                       @db.Timestamptz(6)
  observacao               String?                         @db.VarChar(500)
  created_at               DateTime                        @default(now()) @db.Timestamptz(6)
  updated_at               DateTime?                       @db.Timestamptz(6)
  atendimentos             atendimentos                    @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction)
  pacientes                pacientes                       @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  prestadores              prestadores                     @relation(fields: [solicitante_id], references: [id], onUpdate: NoAction)
  solicitacoes_exame_itens solicitacoes_exame_itens[]

  @@index([atendimento_id, data_solicitacao(sort: Desc)], map: "ix_sol_exame_atend")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model solicitacoes_exame_itens {
  id                                                                              BigInt                        @id @default(autoincrement())
  uuid_externo                                                                    String                        @unique(map: "uq_sol_exame_itens_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                                                       BigInt
  solicitacao_id                                                                  BigInt
  procedimento_id                                                                 BigInt
  observacao                                                                      String?                       @db.VarChar(500)
  status                                                                          enum_solicitacao_exame_status @default(SOLICITADO)
  resultado_id                                                                    BigInt?
  resultados_exame_resultados_exame_solicitacao_item_idTosolicitacoes_exame_itens resultados_exame[]            @relation("resultados_exame_solicitacao_item_idTosolicitacoes_exame_itens")
  resultados_exame_solicitacoes_exame_itens_resultado_idToresultados_exame        resultados_exame?             @relation("solicitacoes_exame_itens_resultado_idToresultados_exame", fields: [resultado_id], references: [id], onUpdate: NoAction, map: "fk_sol_item_resultado")
  tabelas_procedimentos                                                           tabelas_procedimentos         @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)
  solicitacoes_exame                                                              solicitacoes_exame            @relation(fields: [solicitacao_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([solicitacao_id], map: "ix_sol_itens_sol")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cadernos_gabaritos {
  id                        BigInt                     @id @default(autoincrement())
  uuid_externo              String                     @unique(map: "uq_cg_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                 BigInt
  procedimento_principal_id BigInt
  cirurgiao_id              BigInt?
  versao                    Int                        @default(1)
  ativo                     Boolean                    @default(true)
  observacao                String?
  created_at                DateTime                   @default(now()) @db.Timestamptz(6)
  created_by                BigInt?
  updated_at                DateTime?                  @db.Timestamptz(6)
  deleted_at                DateTime?                  @db.Timestamptz(6)
  prestadores               prestadores?               @relation(fields: [cirurgiao_id], references: [id], onUpdate: NoAction)
  tabelas_procedimentos     tabelas_procedimentos      @relation(fields: [procedimento_principal_id], references: [id], onUpdate: NoAction)
  cadernos_gabaritos_itens  cadernos_gabaritos_itens[]
  cirurgias                 cirurgias[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cadernos_gabaritos_itens {
  id                    BigInt                @id @default(autoincrement())
  tenant_id             BigInt
  caderno_id            BigInt
  procedimento_id       BigInt
  quantidade_padrao     Decimal               @db.Decimal(18, 6)
  obrigatorio           Boolean               @default(false)
  observacao            String?               @db.VarChar(300)
  cadernos_gabaritos    cadernos_gabaritos    @relation(fields: [caderno_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tabelas_procedimentos tabelas_procedimentos @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)

  @@unique([caderno_id, procedimento_id], map: "uq_cg_proc")
  @@index([caderno_id], map: "ix_cg_itens_caderno")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This table contains exclusion constraints and requires additional setup for migrations. Visit https://pris.ly/d/exclusion-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cirurgias {
  id                        BigInt                        @id @default(autoincrement())
  uuid_externo              String                        @unique(map: "uq_cirurgias_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                 BigInt
  atendimento_id            BigInt
  paciente_id               BigInt
  procedimento_principal_id BigInt
  procedimentos_secundarios Json?
  sala_id                   BigInt
  data_hora_agendada        DateTime                      @db.Timestamptz(6)
  duracao_estimada_minutos  Int?
  data_hora_inicio          DateTime?                     @db.Timestamptz(6)
  data_hora_fim             DateTime?                     @db.Timestamptz(6)
  cirurgiao_id              BigInt
  tipo_anestesia            enum_cirurgia_tipo_anestesia?
  classificacao_cirurgia    enum_cirurgia_classificacao   @default(ELETIVA)
  kit_cirurgico_id          BigInt?
  caderno_gabarito_id       BigInt?
  ficha_cirurgica           Json?
  ficha_anestesica          Json?
  intercorrencias           String?
  status                    enum_cirurgia_status          @default(AGENDADA)
  conta_id                  BigInt?
  opme_solicitada           Json?
  opme_autorizada           Json?
  opme_utilizada            Json?
  opme_autorizacao_em       DateTime?                     @db.Timestamptz(6)
  opme_autorizacao_por      BigInt?
  cancelado_em              DateTime?                     @db.Timestamptz(6)
  cancelado_por             BigInt?
  cancelamento_motivo       String?                       @db.VarChar(500)
  created_at                DateTime                      @default(now()) @db.Timestamptz(6)
  created_by                BigInt?
  updated_at                DateTime?                     @db.Timestamptz(6)
  deleted_at                DateTime?                     @db.Timestamptz(6)
  atendimentos              atendimentos                  @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction)
  cadernos_gabaritos        cadernos_gabaritos?           @relation(fields: [caderno_gabarito_id], references: [id], onUpdate: NoAction)
  prestadores               prestadores                   @relation(fields: [cirurgiao_id], references: [id], onUpdate: NoAction)
  contas                    contas?                       @relation(fields: [conta_id], references: [id], onUpdate: NoAction)
  kits_cirurgicos           kits_cirurgicos?              @relation(fields: [kit_cirurgico_id], references: [id], onUpdate: NoAction)
  pacientes                 pacientes                     @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  tabelas_procedimentos     tabelas_procedimentos         @relation(fields: [procedimento_principal_id], references: [id], onUpdate: NoAction)
  salas_cirurgicas          salas_cirurgicas              @relation(fields: [sala_id], references: [id], onUpdate: NoAction)
  cirurgias_equipe          cirurgias_equipe[]
  cme_artigos               cme_artigos[]
  repasses_itens            repasses_itens[]

  @@index([atendimento_id], map: "ix_cir_atend")
  @@index([cirurgiao_id, data_hora_agendada(sort: Desc)], map: "ix_cir_cirurgiao")
  @@index([paciente_id], map: "ix_cir_paciente")
  @@index([sala_id, data_hora_agendada], map: "ix_cir_sala_data")
  @@index([tenant_id, status, data_hora_agendada], map: "ix_cir_status")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cirurgias_equipe {
  id            BigInt      @id @default(autoincrement())
  tenant_id     BigInt
  cirurgia_id   BigInt
  prestador_id  BigInt
  funcao        String      @db.VarChar(40)
  ordem         Int         @default(1)
  conta_item_id BigInt?
  cirurgias     cirurgias   @relation(fields: [cirurgia_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  prestadores   prestadores @relation(fields: [prestador_id], references: [id], onUpdate: NoAction)

  @@unique([cirurgia_id, prestador_id, funcao], map: "uq_cir_equipe")
  @@index([cirurgia_id], map: "ix_cir_equipe_cir")
  @@index([prestador_id], map: "ix_cir_equipe_pre")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model contas_itens {
  id                      BigInt                 @id @default(autoincrement())
  uuid_externo            String                 @unique(map: "uq_contas_itens_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               BigInt
  conta_id                BigInt
  procedimento_id         BigInt
  grupo_gasto             enum_grupo_gasto
  origem                  enum_conta_origem_item
  origem_referencia_id    BigInt?
  origem_referencia_tipo  String?                @db.VarChar(40)
  quantidade              Decimal                @db.Decimal(18, 6)
  valor_unitario          Decimal                @default(0) @db.Decimal(18, 6)
  valor_total             Decimal                @default(0) @db.Decimal(18, 4)
  prestador_executante_id BigInt?
  data_realizacao         DateTime?              @db.Timestamptz(6)
  setor_id                BigInt?
  autorizado              Boolean                @default(false)
  numero_autorizacao      String?                @db.VarChar(40)
  fora_pacote             Boolean                @default(false)
  pacote_id               BigInt?
  lote                    String?                @db.VarChar(50)
  validade_lote           DateTime?              @db.Date
  registro_anvisa         String?                @db.VarChar(40)
  fabricante              String?                @db.VarChar(200)
  glosado                 Boolean                @default(false)
  valor_glosa             Decimal                @default(0) @db.Decimal(18, 4)
  guia_tiss_id            BigInt?
  tabela_tiss_origem      String?                @db.VarChar(10)
  created_at              DateTime               @default(now()) @db.Timestamptz(6)
  created_by              BigInt?
  updated_at              DateTime?              @db.Timestamptz(6)
  deleted_at              DateTime?              @db.Timestamptz(6)
  deleted_by              BigInt?
  contas                  contas                 @relation(fields: [conta_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  prestadores             prestadores?           @relation(fields: [prestador_executante_id], references: [id], onUpdate: NoAction)
  tabelas_procedimentos   tabelas_procedimentos  @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)
  setores                 setores?               @relation(fields: [setor_id], references: [id], onUpdate: NoAction)
  guias_tiss              guias_tiss?            @relation(fields: [guia_tiss_id], references: [id], onUpdate: NoAction, map: "fk_contas_itens_guia_tiss")
  pacotes                 pacotes?               @relation(fields: [pacote_id], references: [id], onUpdate: NoAction, map: "fk_contas_itens_pacote")
  glosas                  glosas[]
  repasses_itens          repasses_itens[]

  @@index([origem, origem_referencia_id], map: "ix_ci_origem")
  @@index([procedimento_id], map: "ix_ci_proc")
}

/// This table is a partition table and requires additional setup for migrations. Visit https://pris.ly/d/partition-tables for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dispensacoes {
  id                           BigInt                  @default(autoincrement())
  uuid_externo                 String                  @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                    BigInt
  atendimento_id               BigInt
  paciente_id                  BigInt
  prescricao_id                BigInt?
  prescricao_data_hora         DateTime?               @db.Timestamptz(6)
  cirurgia_id                  BigInt?
  farmaceutico_id              BigInt
  setor_destino_id             BigInt?
  data_hora                    DateTime                @db.Timestamptz(6)
  turno                        String?                 @db.VarChar(20)
  tipo                         enum_dispensacao_tipo
  status                       enum_dispensacao_status @default(PENDENTE)
  observacao                   String?                 @db.VarChar(500)
  dispensacao_origem_id        BigInt?
  dispensacao_origem_data_hora DateTime?               @db.Timestamptz(6)
  created_at                   DateTime                @default(now()) @db.Timestamptz(6)
  created_by                   BigInt?
  updated_at                   DateTime?               @db.Timestamptz(6)
  atendimentos                 atendimentos            @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction)
  prestadores                  prestadores             @relation(fields: [farmaceutico_id], references: [id], onUpdate: NoAction)
  pacientes                    pacientes               @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  setores                      setores?                @relation(fields: [setor_destino_id], references: [id], onUpdate: NoAction)

  @@id([id, data_hora])
  @@unique([uuid_externo, data_hora], map: "uq_dispensacoes_uuid")
  @@index([atendimento_id, data_hora(sort: Desc)], map: "ix_disp_atend_data")
  @@index([paciente_id, data_hora(sort: Desc)], map: "ix_disp_paciente")
  @@index([tenant_id, turno, data_hora(sort: Desc)], map: "ix_disp_turno")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model dispensacoes_itens {
  id                        BigInt                  @id @default(autoincrement())
  uuid_externo              String                  @unique(map: "uq_dispensacoes_itens_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                 BigInt
  dispensacao_id            BigInt
  dispensacao_data_hora     DateTime                @db.Timestamptz(6)
  procedimento_id           BigInt
  prescricao_item_id        BigInt?
  quantidade_prescrita      Decimal                 @db.Decimal(18, 6)
  quantidade_dispensada     Decimal                 @db.Decimal(18, 6)
  unidade_medida            String?                 @db.VarChar(20)
  fator_conversao_aplicado  Decimal?                @db.Decimal(18, 6)
  justificativa_divergencia String?                 @db.VarChar(500)
  lote                      String?                 @db.VarChar(50)
  validade                  DateTime?               @db.Date
  conta_item_id             BigInt?
  status                    enum_dispensacao_status @default(PENDENTE)
  prescricoes_itens         prescricoes_itens?      @relation(fields: [prescricao_item_id], references: [id], onUpdate: NoAction)
  tabelas_procedimentos     tabelas_procedimentos   @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)
  livro_controlados         livro_controlados[]

  @@index([dispensacao_id, dispensacao_data_hora], map: "ix_di_dispensacao")
  @@index([procedimento_id], map: "ix_di_proc")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model kits_cirurgicos {
  id                    BigInt                  @id @default(autoincrement())
  uuid_externo          String                  @unique(map: "uq_kits_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             BigInt
  codigo                String                  @db.VarChar(40)
  nome                  String                  @db.VarChar(200)
  descricao             String?
  ativo                 Boolean                 @default(true)
  created_at            DateTime                @default(now()) @db.Timestamptz(6)
  created_by            BigInt?
  updated_at            DateTime?               @db.Timestamptz(6)
  deleted_at            DateTime?               @db.Timestamptz(6)
  cirurgias             cirurgias[]
  kits_cirurgicos_itens kits_cirurgicos_itens[]

  @@unique([tenant_id, codigo], map: "uq_kits")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model kits_cirurgicos_itens {
  id                    BigInt                @id @default(autoincrement())
  tenant_id             BigInt
  kit_id                BigInt
  procedimento_id       BigInt
  quantidade            Decimal               @db.Decimal(18, 6)
  obrigatorio           Boolean               @default(true)
  kits_cirurgicos       kits_cirurgicos       @relation(fields: [kit_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tabelas_procedimentos tabelas_procedimentos @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)

  @@unique([kit_id, procedimento_id], map: "uq_kit_proc")
  @@index([kit_id], map: "ix_kit_itens_kit")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model livro_controlados {
  id                    BigInt                           @id @default(autoincrement())
  uuid_externo          String                           @unique(map: "uq_livro_controlados_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             BigInt
  data_hora             DateTime                         @db.Timestamptz(6)
  procedimento_id       BigInt
  lote                  String                           @db.VarChar(50)
  quantidade            Decimal                          @db.Decimal(18, 6)
  saldo_anterior        Decimal                          @db.Decimal(18, 6)
  saldo_atual           Decimal                          @db.Decimal(18, 6)
  tipo_movimento        enum_livro_controlados_movimento
  paciente_id           BigInt?
  prescricao_id         BigInt?
  prescricao_data_hora  DateTime?                        @db.Timestamptz(6)
  dispensacao_item_id   BigInt?
  receita_documento_url String?                          @db.VarChar(500)
  farmaceutico_id       BigInt
  observacao            String?                          @db.VarChar(500)
  created_at            DateTime                         @default(now()) @db.Timestamptz(6)
  dispensacoes_itens    dispensacoes_itens?              @relation(fields: [dispensacao_item_id], references: [id], onUpdate: NoAction)
  prestadores           prestadores                      @relation(fields: [farmaceutico_id], references: [id], onUpdate: NoAction)
  pacientes             pacientes?                       @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  tabelas_procedimentos tabelas_procedimentos            @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)

  @@index([lote, data_hora(sort: Desc)], map: "ix_livro_lote_data")
  @@index([procedimento_id, data_hora(sort: Desc)], map: "ix_livro_proc_data")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model glosas {
  id                    BigInt            @id @default(autoincrement())
  uuid_externo          String            @unique(map: "uq_glosas_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             BigInt
  conta_id              BigInt
  conta_item_id         BigInt?
  guia_tiss_id          BigInt?
  convenio_id           BigInt
  motivo                String            @db.VarChar(500)
  codigo_glosa_tiss     String?           @db.VarChar(10)
  valor_glosado         Decimal           @db.Decimal(18, 4)
  data_glosa            DateTime          @db.Date
  origem                enum_glosa_origem @default(TISS)
  prazo_recurso         DateTime?         @db.Date
  recurso               String?
  data_recurso          DateTime?         @db.Date
  recurso_documento_url String?           @db.VarChar(500)
  recurso_por           BigInt?
  status                enum_glosa_status @default(RECEBIDA)
  valor_revertido       Decimal           @default(0) @db.Decimal(18, 4)
  data_resposta_recurso DateTime?         @db.Date
  motivo_resposta       String?           @db.VarChar(500)
  created_at            DateTime          @default(now()) @db.Timestamptz(6)
  created_by            BigInt?
  updated_at            DateTime?         @db.Timestamptz(6)
  contas                contas            @relation(fields: [conta_id], references: [id], onUpdate: NoAction)
  contas_itens          contas_itens?     @relation(fields: [conta_item_id], references: [id], onUpdate: NoAction)
  convenios             convenios         @relation(fields: [convenio_id], references: [id], onUpdate: NoAction)
  guias_tiss            guias_tiss?       @relation(fields: [guia_tiss_id], references: [id], onUpdate: NoAction)
  usuarios              Usuario?          @relation(fields: [recurso_por], references: [id], onUpdate: NoAction)

  @@index([conta_id], map: "ix_glosas_conta")
  @@index([tenant_id, status, data_glosa(sort: Desc)], map: "ix_glosas_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model guias_tiss {
  id                    BigInt                @id @default(autoincrement())
  uuid_externo          String                @unique(map: "uq_guias_tiss_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             BigInt
  conta_id              BigInt
  lote_id               BigInt?
  tipo_guia             enum_guia_tiss_tipo
  versao_tiss           String                @db.VarChar(10)
  numero_guia_prestador String                @db.VarChar(30)
  numero_guia_operadora String?               @db.VarChar(30)
  senha_autorizacao     String?               @db.VarChar(40)
  xml_conteudo          String?
  hash_xml              String?               @db.VarChar(64)
  valor_total           Decimal               @default(0) @db.Decimal(18, 4)
  status                enum_guia_tiss_status @default(GERADA)
  validacao_xsd_status  String?               @db.VarChar(20)
  validacao_xsd_erros   Json?
  data_geracao          DateTime              @default(now()) @db.Timestamptz(6)
  data_validacao        DateTime?             @db.Timestamptz(6)
  data_envio            DateTime?             @db.Timestamptz(6)
  data_resposta         DateTime?             @db.Timestamptz(6)
  motivo_recusa         String?               @db.VarChar(500)
  created_at            DateTime              @default(now()) @db.Timestamptz(6)
  created_by            BigInt?
  contas_itens          contas_itens[]
  glosas                glosas[]
  contas                contas                @relation(fields: [conta_id], references: [id], onUpdate: NoAction)
  lotes_tiss            lotes_tiss?           @relation(fields: [lote_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, numero_guia_prestador], map: "uq_guias_numero")
  @@index([conta_id], map: "ix_guias_conta")
  @@index([tenant_id, status, data_geracao(sort: Desc)], map: "ix_guias_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model lotes_tiss {
  id                  BigInt                @id @default(autoincrement())
  uuid_externo        String                @unique(map: "uq_lotes_tiss_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id           BigInt
  convenio_id         BigInt
  numero_lote         String                @db.VarChar(20)
  versao_tiss         String                @db.VarChar(10)
  competencia         String                @db.VarChar(7)
  data_geracao        DateTime              @default(now()) @db.Timestamptz(6)
  data_validacao      DateTime?             @db.Timestamptz(6)
  data_envio          DateTime?             @db.Timestamptz(6)
  data_processamento  DateTime?             @db.Timestamptz(6)
  qtd_guias           Int                   @default(0)
  valor_total         Decimal               @default(0) @db.Decimal(18, 4)
  hash_xml            String?               @db.VarChar(64)
  xml_url             String?               @db.VarChar(500)
  protocolo_operadora String?               @db.VarChar(40)
  status              enum_lote_tiss_status @default(EM_PREPARACAO)
  validacao_xsd_erros Json?
  lote_anterior_id    BigInt?
  observacao          String?
  created_at          DateTime              @default(now()) @db.Timestamptz(6)
  created_by          BigInt?
  updated_at          DateTime?             @db.Timestamptz(6)
  guias_tiss          guias_tiss[]
  convenios           convenios             @relation(fields: [convenio_id], references: [id], onUpdate: NoAction)
  lotes_tiss          lotes_tiss?           @relation("lotes_tissTolotes_tiss", fields: [lote_anterior_id], references: [id], onUpdate: NoAction)
  other_lotes_tiss    lotes_tiss[]          @relation("lotes_tissTolotes_tiss")

  @@unique([tenant_id, convenio_id, numero_lote], map: "uq_lote")
  @@index([convenio_id, competencia], map: "ix_lotes_convenio_comp")
  @@index([tenant_id, status, data_geracao(sort: Desc)], map: "ix_lotes_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model pacotes {
  id                        BigInt                 @id @default(autoincrement())
  uuid_externo              String                 @unique(map: "uq_pacotes_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                 BigInt
  codigo                    String                 @db.VarChar(40)
  nome                      String                 @db.VarChar(300)
  descricao                 String?
  procedimento_principal_id BigInt?
  convenio_id               BigInt?
  valor_total               Decimal                @db.Decimal(18, 4)
  vigencia_inicio           DateTime               @db.Date
  vigencia_fim              DateTime?              @db.Date
  ativo                     Boolean                @default(true)
  created_at                DateTime               @default(now()) @db.Timestamptz(6)
  created_by                BigInt?
  updated_at                DateTime?              @db.Timestamptz(6)
  deleted_at                DateTime?              @db.Timestamptz(6)
  contas_itens              contas_itens[]
  convenios                 convenios?             @relation(fields: [convenio_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  tabelas_procedimentos     tabelas_procedimentos? @relation(fields: [procedimento_principal_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  pacotes_itens             pacotes_itens[]

  @@unique([tenant_id, codigo], map: "uq_pacotes")
  @@index([procedimento_principal_id, vigencia_inicio(sort: Desc)], map: "ix_pacotes_proc_princ")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model pacotes_itens {
  id                    BigInt                @id @default(autoincrement())
  tenant_id             BigInt
  pacote_id             BigInt
  procedimento_id       BigInt
  quantidade            Decimal               @default(1) @db.Decimal(18, 6)
  faixa_inicio          String?               @db.VarChar(20)
  faixa_fim             String?               @db.VarChar(20)
  pacotes               pacotes               @relation(fields: [pacote_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  tabelas_procedimentos tabelas_procedimentos @relation(fields: [procedimento_id], references: [id], onUpdate: NoAction)

  @@unique([pacote_id, procedimento_id], map: "uq_pi")
  @@index([pacote_id], map: "ix_pi_pacote")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model criterios_repasse {
  id                     BigInt                         @id @default(autoincrement())
  uuid_externo           String                         @unique(map: "uq_criterios_repasse_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id              BigInt
  descricao              String                         @db.VarChar(200)
  vigencia_inicio        DateTime                       @db.Date
  vigencia_fim           DateTime?                      @db.Date
  unidade_faturamento_id BigInt?
  unidade_atendimento_id BigInt?
  tipo_base_calculo      enum_repasse_tipo_base_calculo
  momento_repasse        enum_repasse_momento
  dia_fechamento         Int?
  prazo_dias             Int?
  prioridade             Int                            @default(1)
  regras                 Json
  ativo                  Boolean                        @default(true)
  created_at             DateTime                       @default(now()) @db.Timestamptz(6)
  created_by             BigInt?
  updated_at             DateTime?                      @db.Timestamptz(6)
  deleted_at             DateTime?                      @db.Timestamptz(6)
  unidades_atendimento   unidades_atendimento?          @relation(fields: [unidade_atendimento_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  unidades_faturamento   unidades_faturamento?          @relation(fields: [unidade_faturamento_id], references: [id], onDelete: Restrict, onUpdate: NoAction)
  repasses_itens         repasses_itens[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model repasses {
  id                                        BigInt              @id @default(autoincrement())
  uuid_externo                              String              @unique(map: "uq_repasses_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                 BigInt
  prestador_id                              BigInt
  competencia                               String              @db.VarChar(7)
  data_apuracao                             DateTime            @default(now()) @db.Timestamptz(6)
  data_conferencia                          DateTime?           @db.Timestamptz(6)
  conferido_por                             BigInt?
  data_liberacao                            DateTime?           @db.Timestamptz(6)
  liberado_por                              BigInt?
  data_pagamento                            DateTime?           @db.Timestamptz(6)
  pago_por                                  BigInt?
  valor_bruto                               Decimal             @default(0) @db.Decimal(18, 4)
  valor_creditos                            Decimal             @default(0) @db.Decimal(18, 4)
  valor_debitos                             Decimal             @default(0) @db.Decimal(18, 4)
  valor_descontos                           Decimal             @default(0) @db.Decimal(18, 4)
  valor_impostos                            Decimal             @default(0) @db.Decimal(18, 4)
  valor_liquido                             Decimal             @default(0) @db.Decimal(18, 4)
  status                                    enum_repasse_status @default(APURADO)
  cancelado_em                              DateTime?           @db.Timestamptz(6)
  cancelado_motivo                          String?             @db.VarChar(500)
  observacao                                String?
  created_at                                DateTime            @default(now()) @db.Timestamptz(6)
  created_by                                BigInt?
  updated_at                                DateTime?           @db.Timestamptz(6)
  usuarios_repasses_conferido_porTousuarios Usuario?            @relation("repasses_conferido_porTousuarios", fields: [conferido_por], references: [id], onUpdate: NoAction)
  usuarios_repasses_liberado_porTousuarios  Usuario?            @relation("repasses_liberado_porTousuarios", fields: [liberado_por], references: [id], onUpdate: NoAction)
  usuarios_repasses_pago_porTousuarios      Usuario?            @relation("repasses_pago_porTousuarios", fields: [pago_por], references: [id], onUpdate: NoAction)
  prestadores                               prestadores         @relation(fields: [prestador_id], references: [id], onUpdate: NoAction)
  repasses_itens                            repasses_itens[]

  @@unique([tenant_id, prestador_id, competencia], map: "uq_repasse")
  @@index([tenant_id, competencia], map: "ix_repasses_competencia")
  @@index([prestador_id, competencia(sort: Desc)], map: "ix_repasses_prestador")
  @@index([tenant_id, status, competencia(sort: Desc)], map: "ix_repasses_status")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model repasses_itens {
  id                   BigInt             @id @default(autoincrement())
  uuid_externo         String             @unique(map: "uq_repasses_itens_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id            BigInt
  repasse_id           BigInt
  conta_id             BigInt
  conta_item_id        BigInt?
  cirurgia_id          BigInt?
  criterio_id          BigInt?
  funcao               String?            @db.VarChar(40)
  base_calculo         Decimal            @db.Decimal(18, 4)
  percentual           Decimal?           @db.Decimal(7, 4)
  valor_fixo           Decimal?           @db.Decimal(18, 4)
  valor_calculado      Decimal            @db.Decimal(18, 4)
  criterio_snapshot    Json?
  reapurado_de_id      BigInt?
  glosado              Boolean            @default(false)
  observacao           String?            @db.VarChar(500)
  created_at           DateTime           @default(now()) @db.Timestamptz(6)
  cirurgias            cirurgias?         @relation(fields: [cirurgia_id], references: [id], onUpdate: NoAction)
  contas               contas             @relation(fields: [conta_id], references: [id], onUpdate: NoAction)
  contas_itens         contas_itens?      @relation(fields: [conta_item_id], references: [id], onUpdate: NoAction)
  criterios_repasse    criterios_repasse? @relation(fields: [criterio_id], references: [id], onUpdate: NoAction)
  repasses_itens       repasses_itens?    @relation("repasses_itensTorepasses_itens", fields: [reapurado_de_id], references: [id], onUpdate: NoAction)
  other_repasses_itens repasses_itens[]   @relation("repasses_itensTorepasses_itens")
  repasses             repasses           @relation(fields: [repasse_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([conta_id], map: "ix_ri_conta")
  @@index([repasse_id], map: "ix_ri_repasse")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model ccih_casos {
  id                      BigInt                    @id @default(autoincrement())
  uuid_externo            String                    @unique(map: "uq_ccih_casos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               BigInt
  paciente_id             BigInt
  atendimento_id          BigInt
  setor_id                BigInt
  leito_id                BigInt?
  data_diagnostico        DateTime                  @db.Date
  topografia              String?                   @db.VarChar(80)
  cid                     String?                   @db.VarChar(10)
  microorganismo          String?                   @db.VarChar(120)
  cultura_origem          String?                   @db.VarChar(80)
  resistencia             Json?
  origem_infeccao         enum_ccih_origem_infeccao @default(INDETERMINADA)
  notificacao_compulsoria Boolean                   @default(false)
  data_notificacao        DateTime?                 @db.Timestamptz(6)
  resultado               String?                   @db.VarChar(40)
  status                  enum_ccih_caso_status     @default(ABERTO)
  observacao              String?
  created_at              DateTime                  @default(now()) @db.Timestamptz(6)
  created_by              BigInt?
  updated_at              DateTime?                 @db.Timestamptz(6)
  deleted_at              DateTime?                 @db.Timestamptz(6)
  atendimentos            atendimentos              @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction)
  leitos                  leitos?                   @relation(fields: [leito_id], references: [id], onUpdate: NoAction)
  pacientes               pacientes                 @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  setores                 setores                   @relation(fields: [setor_id], references: [id], onUpdate: NoAction)

  @@index([paciente_id, data_diagnostico(sort: Desc)], map: "ix_ccih_paciente")
  @@index([tenant_id, status, data_diagnostico(sort: Desc)], map: "ix_ccih_status")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cme_artigos {
  id                  BigInt              @id @default(autoincrement())
  uuid_externo        String              @unique(map: "uq_cme_artigos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id           BigInt
  lote_id             BigInt
  codigo_artigo       String              @db.VarChar(60)
  descricao           String?             @db.VarChar(300)
  etapa_atual         enum_cme_etapa      @default(RECEPCAO)
  cirurgia_id         BigInt?
  paciente_id         BigInt?
  ultima_movimentacao DateTime            @default(now()) @db.Timestamptz(6)
  created_at          DateTime            @default(now()) @db.Timestamptz(6)
  created_by          BigInt?
  updated_at          DateTime?           @db.Timestamptz(6)
  cirurgias           cirurgias?          @relation(fields: [cirurgia_id], references: [id], onUpdate: NoAction)
  cme_lotes           cme_lotes           @relation(fields: [lote_id], references: [id], onUpdate: NoAction)
  pacientes           pacientes?          @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  cme_movimentacoes   cme_movimentacoes[]

  @@index([tenant_id, etapa_atual], map: "ix_cme_artigos_etapa")
  @@index([lote_id], map: "ix_cme_artigos_lote")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cme_lotes {
  id                      BigInt                        @id @default(autoincrement())
  uuid_externo            String                        @unique(map: "uq_cme_lotes_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               BigInt
  numero                  String                        @db.VarChar(40)
  metodo                  enum_cme_metodo_esterilizacao
  data_esterilizacao      DateTime                      @db.Timestamptz(6)
  validade                DateTime                      @db.Date
  responsavel_id          BigInt
  indicador_biologico_url String?                       @db.VarChar(500)
  indicador_quimico_ok    Boolean?
  indicador_biologico_ok  Boolean?
  data_liberacao          DateTime?                     @db.Timestamptz(6)
  liberado_por            BigInt?
  data_reprovacao         DateTime?                     @db.Timestamptz(6)
  motivo_reprovacao       String?                       @db.VarChar(500)
  status                  enum_cme_lote_status          @default(EM_PROCESSAMENTO)
  observacao              String?
  created_at              DateTime                      @default(now()) @db.Timestamptz(6)
  created_by              BigInt?
  updated_at              DateTime?                     @db.Timestamptz(6)
  deleted_at              DateTime?                     @db.Timestamptz(6)
  ativo                   Boolean                       @default(true)
  cme_artigos             cme_artigos[]
  usuarios                Usuario?                      @relation(fields: [liberado_por], references: [id], onUpdate: NoAction)
  prestadores             prestadores                   @relation(fields: [responsavel_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, numero], map: "uq_cme_lote_numero")
  @@index([tenant_id, status, data_esterilizacao(sort: Desc)], map: "ix_cme_lotes_status")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cme_movimentacoes {
  id             BigInt          @id @default(autoincrement())
  uuid_externo   String          @unique(map: "uq_cme_movs_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id      BigInt
  artigo_id      BigInt
  etapa_origem   enum_cme_etapa?
  etapa_destino  enum_cme_etapa
  responsavel_id BigInt
  data_hora      DateTime        @default(now()) @db.Timestamptz(6)
  observacao     String?         @db.VarChar(500)
  created_at     DateTime        @default(now()) @db.Timestamptz(6)
  cme_artigos    cme_artigos     @relation(fields: [artigo_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  prestadores    prestadores     @relation(fields: [responsavel_id], references: [id], onUpdate: NoAction)

  @@index([artigo_id, data_hora(sort: Desc)], map: "ix_cme_movs_artigo")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model same_emprestimos {
  id                      BigInt                      @id @default(autoincrement())
  uuid_externo            String                      @unique(map: "uq_same_emprestimos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id               BigInt
  prontuario_id           BigInt
  solicitante_id          BigInt
  data_emprestimo         DateTime                    @default(now()) @db.Timestamptz(6)
  data_devolucao_prevista DateTime                    @db.Date
  data_devolucao_real     DateTime?                   @db.Timestamptz(6)
  motivo                  String                      @db.VarChar(200)
  status                  enum_same_emprestimo_status @default(ATIVO)
  observacao              String?
  created_at              DateTime                    @default(now()) @db.Timestamptz(6)
  same_prontuarios        same_prontuarios            @relation(fields: [prontuario_id], references: [id], onUpdate: NoAction)
  usuarios                Usuario                     @relation(fields: [solicitante_id], references: [id], onUpdate: NoAction)

  @@index([prontuario_id, data_emprestimo(sort: Desc)], map: "ix_same_emp_prontuario")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model same_prontuarios {
  id                 BigInt                      @id @default(autoincrement())
  uuid_externo       String                      @unique(map: "uq_same_prontuarios_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          BigInt
  paciente_id        BigInt
  numero_pasta       String                      @db.VarChar(40)
  localizacao        String?                     @db.VarChar(200)
  status             enum_same_prontuario_status @default(ARQUIVADO)
  digitalizado       Boolean                     @default(false)
  pdf_legado_url     String?                     @db.VarChar(500)
  data_digitalizacao DateTime?                   @db.Timestamptz(6)
  digitalizado_por   BigInt?
  observacao         String?
  created_at         DateTime                    @default(now()) @db.Timestamptz(6)
  created_by         BigInt?
  updated_at         DateTime?                   @db.Timestamptz(6)
  same_emprestimos   same_emprestimos[]
  usuarios           Usuario?                    @relation(fields: [digitalizado_por], references: [id], onUpdate: NoAction)
  pacientes          pacientes                   @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, paciente_id], map: "uq_same_paciente")
  @@unique([tenant_id, numero_pasta], map: "uq_same_pasta")
  @@index([tenant_id, status], map: "ix_same_status")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model visitantes {
  id                 BigInt    @id @default(autoincrement())
  uuid_externo       String    @unique(map: "uq_visitantes_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          BigInt
  nome               String    @db.VarChar(300)
  cpf_hash           String    @db.VarChar(64)
  cpf_ultimos4       String?   @db.VarChar(4)
  documento_foto_url String?   @db.VarChar(500)
  bloqueado          Boolean   @default(false)
  motivo_bloqueio    String?   @db.VarChar(200)
  bloqueado_em       DateTime? @db.Timestamptz(6)
  bloqueado_por      BigInt?
  observacao         String?
  created_at         DateTime  @default(now()) @db.Timestamptz(6)
  created_by         BigInt?
  updated_at         DateTime? @db.Timestamptz(6)
  deleted_at         DateTime? @db.Timestamptz(6)
  usuarios           Usuario?  @relation(fields: [bloqueado_por], references: [id], onUpdate: NoAction)
  visitas            visitas[]

  @@unique([tenant_id, cpf_hash], map: "uq_visitante_cpf")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model visitas {
  id             BigInt        @id @default(autoincrement())
  uuid_externo   String        @unique(map: "uq_visitas_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id      BigInt
  visitante_id   BigInt
  paciente_id    BigInt
  atendimento_id BigInt?
  leito_id       BigInt?
  setor_id       BigInt?
  data_entrada   DateTime      @default(now()) @db.Timestamptz(6)
  data_saida     DateTime?     @db.Timestamptz(6)
  porteiro_id    BigInt?
  observacao     String?       @db.VarChar(500)
  created_at     DateTime      @default(now()) @db.Timestamptz(6)
  atendimentos   atendimentos? @relation(fields: [atendimento_id], references: [id], onUpdate: NoAction)
  leitos         leitos?       @relation(fields: [leito_id], references: [id], onUpdate: NoAction)
  pacientes      pacientes     @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)
  usuarios       Usuario?      @relation(fields: [porteiro_id], references: [id], onUpdate: NoAction)
  setores        setores?      @relation(fields: [setor_id], references: [id], onUpdate: NoAction)
  visitantes     visitantes    @relation(fields: [visitante_id], references: [id], onUpdate: NoAction)

  @@index([paciente_id, data_entrada(sort: Desc)], map: "ix_visitas_paciente")
  @@index([visitante_id, data_entrada(sort: Desc)], map: "ix_visitas_visitante")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model consentimentos_lgpd {
  id                BigInt                        @id @default(autoincrement())
  uuid_externo      String                        @unique(map: "uq_consentimentos_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id         BigInt
  paciente_id       BigInt
  finalidade        enum_consentimento_finalidade
  texto_apresentado String
  versao_termo      String                        @db.VarChar(20)
  aceito            Boolean
  data_decisao      DateTime                      @default(now()) @db.Timestamptz(6)
  ip_origem         String?                       @db.Inet
  user_agent        String?                       @db.VarChar(500)
  data_revogacao    DateTime?                     @db.Timestamptz(6)
  motivo_revogacao  String?                       @db.VarChar(500)
  created_at        DateTime                      @default(now()) @db.Timestamptz(6)
  created_by        BigInt?
  pacientes         pacientes                     @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)

  @@unique([tenant_id, paciente_id, finalidade, versao_termo], map: "uq_consent_paciente_finalidade")
  @@index([paciente_id, finalidade], map: "ix_consent_paciente")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notificacoes_paciente {
  id              BigInt                  @id @default(autoincrement())
  uuid_externo    String                  @unique(map: "uq_notificacoes_paciente_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id       BigInt
  paciente_id     BigInt
  canal           enum_notificacao_canal
  destinatario    String                  @db.VarChar(200)
  assunto         String?                 @db.VarChar(200)
  conteudo        String
  payload_extra   Json?
  template_codigo String?                 @db.VarChar(80)
  status          enum_notificacao_status @default(PENDENTE)
  data_envio      DateTime?               @db.Timestamptz(6)
  data_entrega    DateTime?               @db.Timestamptz(6)
  data_leitura    DateTime?               @db.Timestamptz(6)
  erro_mensagem   String?                 @db.VarChar(500)
  tentativas      Int                     @default(0)
  provider_id     String?                 @db.VarChar(120)
  origem_evento   String?                 @db.VarChar(80)
  created_at      DateTime                @default(now()) @db.Timestamptz(6)
  created_by      BigInt?
  pacientes       pacientes               @relation(fields: [paciente_id], references: [id], onUpdate: NoAction)

  @@index([paciente_id, created_at(sort: Desc)], map: "ix_notif_paciente")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model webhooks_inbox {
  id                 BigInt              @id @default(autoincrement())
  uuid_externo       String              @unique(map: "uq_webhooks_inbox_uuid") @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          BigInt
  origem             enum_webhook_origem
  idempotency_key    String              @db.VarChar(120)
  endpoint           String              @db.VarChar(200)
  payload            Json
  headers            Json?
  signature          String?             @db.VarChar(500)
  status             enum_webhook_status @default(RECEBIDO)
  data_recebimento   DateTime            @default(now()) @db.Timestamptz(6)
  data_processamento DateTime?           @db.Timestamptz(6)
  tentativas         Int                 @default(0)
  erro_mensagem      String?
  erro_stack         String?
  resultado          Json?
  created_at         DateTime            @default(now()) @db.Timestamptz(6)

  @@unique([tenant_id, origem, idempotency_key], map: "uq_webhook_idem")
  @@index([origem, data_recebimento(sort: Desc)], map: "ix_webhooks_origem_data")
}

enum enum_convenio_tipo {
  CONVENIO
  SUS
  PARTICULAR
}

enum enum_grupo_gasto {
  PROCEDIMENTO
  DIARIA
  TAXA
  SERVICO
  MATERIAL
  MEDICAMENTO
  OPME
  GAS
  PACOTE
  HONORARIO
}

enum enum_leito_status {
  DISPONIVEL
  OCUPADO
  RESERVADO
  HIGIENIZACAO
  MANUTENCAO
  BLOQUEADO
}

enum enum_leito_tipo_acomodacao {
  ENFERMARIA
  APARTAMENTO
  UTI
  SEMI_UTI
  ISOLAMENTO
  OBSERVACAO
}

enum enum_paciente_sexo {
  M
  F
  INDETERMINADO
}

enum enum_paciente_tipo_atendimento_padrao {
  PARTICULAR
  CONVENIO
  SUS
}

enum enum_prestador_tipo_conselho {
  CRM
  COREN
  CRF
  CRN
  CREFITO
  CRP
  CRO
  CRBM
  CRFa
  OUTROS
}

enum enum_prestador_tipo_vinculo {
  CORPO_CLINICO
  PLANTONISTA
  COOPERADO
  TERCEIRO
  CLT
}

enum enum_procedimento_tipo {
  PROCEDIMENTO
  DIARIA
  TAXA
  SERVICO
  MATERIAL
  MEDICAMENTO
  OPME
  GAS
  PACOTE
}

enum enum_setor_tipo {
  INTERNACAO
  AMBULATORIO
  PRONTO_SOCORRO
  CENTRO_CIRURGICO
  UTI
  CME
  FARMACIA
  LABORATORIO
  IMAGEM
  ADMINISTRATIVO
}

enum enum_lgpd_solicitacao_tipo {
  ACESSO
  EXCLUSAO
  PORTABILIDADE
  CORRECAO
}

enum enum_lgpd_solicitacao_status {
  PENDENTE
  EM_ANALISE
  ATENDIDA
  NEGADA
}

enum enum_agenda_recurso_tipo {
  PRESTADOR
  SALA
  EQUIPAMENTO
}

enum enum_agendamento_origem {
  INTERNO
  PORTAL
  TOTEM
  TELEFONE
  API
}

enum enum_agendamento_status {
  AGENDADO
  CONFIRMADO
  COMPARECEU
  FALTOU
  CANCELADO
  REAGENDADO
}

enum enum_atendimento_tipo {
  CONSULTA
  EXAME
  INTERNACAO
  CIRURGIA
  PRONTO_ATENDIMENTO
  TELECONSULTA
  OBSERVACAO
}

enum enum_atendimento_classificacao_risco {
  VERMELHO
  LARANJA
  AMARELO
  VERDE
  AZUL
}

enum enum_atendimento_status {
  AGENDADO
  EM_ESPERA
  EM_TRIAGEM
  EM_ATENDIMENTO
  INTERNADO
  ALTA
  CANCELADO
  NAO_COMPARECEU
}

enum enum_atendimento_tipo_alta {
  ALTA_MEDICA
  ALTA_PEDIDO
  TRANSFERENCIA
  EVASAO
  OBITO
}

enum enum_conta_status {
  ABERTA
  EM_ELABORACAO
  FECHADA
  FATURADA
  GLOSADA_PARCIAL
  GLOSADA_TOTAL
  PAGA
  CANCELADA
}

enum enum_tipo_cobranca {
  PARTICULAR
  CONVENIO
  SUS
}

enum enum_analise_farmaceutica_status {
  APROVADA
  RECUSADA
  APROVADA_RESSALVAS
  PENDENTE
}

enum enum_documento_tipo {
  ATESTADO
  RECEITA_SIMPLES
  RECEITA_CONTROLADO
  DECLARACAO
  ENCAMINHAMENTO
  RESUMO_ALTA
  OUTRO
}

enum enum_evolucao_tipo {
  ANAMNESE
  EXAME_CLINICO
  EVOLUCAO
  NOTA_ADMISSAO
  NOTA_ALTA
  PARECER
  INTERCONSULTA
  RESUMO_ALTA
  RETIFICACAO
}

enum enum_evolucao_tipo_profissional {
  MEDICO
  ENFERMEIRO
  TECNICO_ENFERMAGEM
  NUTRICIONISTA
  FISIOTERAPEUTA
  PSICOLOGO
  FARMACEUTICO
  FONOAUDIOLOGO
  TERAPEUTA_OCUPACIONAL
  ASSISTENTE_SOCIAL
  OUTROS
}

enum enum_interacao_severidade {
  LEVE
  MODERADA
  GRAVE
  CONTRAINDICADA
}

enum enum_prescricao_status {
  AGUARDANDO_ANALISE
  ATIVA
  SUSPENSA
  CANCELADA
  ENCERRADA
  RECUSADA_FARMACIA
}

enum enum_prescricao_tipo {
  MEDICAMENTO
  CUIDADO
  DIETA
  PROCEDIMENTO
  EXAME
  COMPOSTA
}

enum enum_solicitacao_exame_status {
  SOLICITADO
  AUTORIZADO
  COLETADO
  EM_PROCESSAMENTO
  LAUDO_PARCIAL
  LAUDO_FINAL
  CANCELADO
  NEGADO
}

enum enum_solicitacao_exame_urgencia {
  ROTINA
  URGENTE
  EMERGENCIA
}

enum enum_cirurgia_classificacao {
  ELETIVA
  URGENCIA
  EMERGENCIA
}

enum enum_cirurgia_status {
  AGENDADA
  CONFIRMADA
  EM_ANDAMENTO
  CONCLUIDA
  CANCELADA
  SUSPENSA
}

enum enum_cirurgia_tipo_anestesia {
  GERAL
  RAQUIDIANA
  PERIDURAL
  BLOQUEIO
  LOCAL
  SEDACAO
  NENHUMA
}

enum enum_conta_origem_item {
  PEP
  PRESCRICAO
  CIRURGIA
  EXAME
  MANUAL
  FARMACIA
  PACOTE
}

enum enum_dispensacao_status {
  PENDENTE
  SEPARADA
  DISPENSADA
  DEVOLVIDA
  CANCELADA
}

enum enum_dispensacao_tipo {
  PRESCRICAO
  AVULSA
  KIT_CIRURGICO
  DEVOLUCAO
}

enum enum_livro_controlados_movimento {
  ENTRADA
  SAIDA
  AJUSTE
  PERDA
}

enum enum_glosa_origem {
  TISS
  MANUAL
}

enum enum_glosa_status {
  RECEBIDA
  EM_ANALISE
  EM_RECURSO
  ACATADA
  REVERTIDA_TOTAL
  REVERTIDA_PARCIAL
  PERDA_DEFINITIVA
}

enum enum_guia_tiss_status {
  GERADA
  VALIDADA
  ENVIADA
  ACEITA
  RECUSADA
  GLOSADA
}

enum enum_guia_tiss_tipo {
  CONSULTA
  SP_SADT
  INTERNACAO
  HONORARIOS
  OUTRAS_DESPESAS
  RESUMO_INTERNACAO
  ANEXO_OPME
}

enum enum_lote_tiss_status {
  EM_PREPARACAO
  GERADO
  VALIDADO
  ENVIADO
  PROCESSADO
  COM_ERRO
}

enum enum_repasse_momento {
  AO_FATURAR
  AO_CONFIRMAR_RECEBIMENTO
  COM_PRAZO_DEFINIDO
}

enum enum_repasse_status {
  APURADO
  CONFERIDO
  LIBERADO
  PAGO
  CANCELADO
}

enum enum_repasse_tipo_base_calculo {
  VALOR_TOTAL
  VALOR_COM_DEDUCOES
  VALOR_COM_ACRESCIMOS
  VALOR_LIQUIDO_PAGO
}

enum enum_ccih_caso_status {
  ABERTO
  EM_TRATAMENTO
  NOTIFICADO
  ENCERRADO
  CANCELADO
}

enum enum_ccih_origem_infeccao {
  COMUNITARIA
  HOSPITALAR
  INDETERMINADA
}

enum enum_cme_etapa {
  RECEPCAO
  LIMPEZA
  PREPARO
  ESTERILIZACAO
  GUARDA
  DISTRIBUICAO
  EM_USO
  DESCARTADO
}

enum enum_cme_lote_status {
  EM_PROCESSAMENTO
  AGUARDANDO_INDICADOR
  LIBERADO
  REPROVADO
  EXPIRADO
}

enum enum_cme_metodo_esterilizacao {
  AUTOCLAVE
  OXIDO_ETILENO
  PLASMA
  OZONIO
  QUIMICO_LIQUIDO
}

enum enum_same_emprestimo_status {
  ATIVO
  DEVOLVIDO
  ATRASADO
}

enum enum_same_prontuario_status {
  ARQUIVADO
  EMPRESTADO
  DIGITALIZADO
  DESCARTADO
}

enum enum_consentimento_finalidade {
  TERMO_USO_PORTAL
  COMPARTILHAMENTO_DADOS_CONVENIO
  TELECONSULTA_GRAVACAO
  PESQUISA_CIENTIFICA
  COMUNICACAO_MARKETING
  OUTROS
}

enum enum_notificacao_canal {
  EMAIL
  SMS
  PUSH
  WHATSAPP
}

enum enum_notificacao_status {
  PENDENTE
  ENVIADA
  ENTREGUE
  LIDA
  FALHA
}

enum enum_usuario_tipo_perfil {
  INTERNO
  PRESTADOR
  PACIENTE
}

enum enum_webhook_origem {
  TISS_RETORNO
  LAB_APOIO
  FINANCEIRO
  GATEWAY_PAGAMENTO
  OUTROS
}

enum enum_webhook_status {
  RECEBIDO
  PROCESSANDO
  PROCESSADO
  ERRO
  IGNORADO
}
